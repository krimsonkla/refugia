{"id": "fnd-001", "kind": "finding", "title": "No LICENSE, so a public repo grants nobody the right to fork it", "status": "fixed", "severity": "P1", "release_gate": "blocker", "effort": "trivial", "lens": "contributor-docs", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-009", "fnd-032", "fnd-033"], "evidence": ["absent from all 68 tracked files", "gh api repos/krimsonkla/refugia -> \"license\": null", "pyproject.toml:1-10 has no license, readme, authors or urls", "LICENSE added — MIT, copyright the author, 2026", "pyproject.toml: license = \"MIT\", license-files = [\"LICENSE\"], readme = \"README.md\"", "uv rebuilt the project against the new metadata without complaint"], "updated": "2026-09-07", "notes": "Add a top-level LICENSE and set license + license-files in pyproject.toml [project]. Publishing without one is all-rights-reserved, so the stated goal of an open project is not achieved. Recommendation: MIT — Apache-2.0's patent grant buys little on a 1,345-statement personal tool with no patentable surface. Unblocks the README 'License and data' section the attribution work hangs off. FIXED: MIT, chosen by the owner. The copyright line carries the author's real name, which is standard for OSS and already public from the commit authorship — see fnd-003, which deliberately does not rewrite it.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-002", "kind": "finding", "title": "The committed personal profile cannot simply be deleted — it is the only one the docs run", "status": "fixed", "severity": "P0", "release_gate": "blocker", "effort": "small", "lens": "personal-data", "cycle": "cyc-release-audit", "refs": ["README.md#use"], "depends_on": [], "blocked_by": [], "related": ["fnd-003", "fnd-011", "fnd-022", "fnd-050"], "evidence": ["the author's personal profile under profiles/ — a name field carrying his given name, and his real home_fips", "the only file under profiles/", "README.md:50-51", "cli.py:107 and :135 — --profile has no default", "tests/test_profile.py:45-46 round-tripped that same home FIPS", "README.md:66-67 and :144-145 named the author's home county", "the personal profile removed; profiles/example.json added", "example weights all 21 registered metrics — verified against build_registry(), no unknown and no unweighted keys", "home_fips is 17031 (Cook County, Illinois) — a deliberate placeholder, not the owner's county", "tests/test_profile.py now round-trips 17031", "README.md repointed; both home-county passages rewritten to keep their measurements and drop the county", ".gitignore: profiles/*.json with !profiles/example.json — verified a new profiles/*.json is ignored while the example stays tracked", "a grep for the county name, its FIPS and the author's given name returns nothing across the tree"], "updated": "2026-09-07", "notes": "One commit: git rm the personal profile; add profiles/example.json (neutral name, no home_fips, a weight per registered metric, one illustrative criteria entry, percentile normalization, min_coverage 0.8); repoint README.md:50-51; add 'profiles/*.json' plus '!profiles/example.json' to .gitignore; change the test FIPS to 00001 (the test only asserts round-tripping); rewrite the two home-county passages in the register city_profile.py:52-53 already models. Deleting without a replacement breaks 2 of the 4 documented commands. home_fips degrades gracefully when absent — verified at profile.py:24,43 and template.html:1139,1364-1365. Target state: the county name, its FIPS and the author's given name appear nowhere. FIXED. The two README passages were the subtle part: they are worked examples whose numbers are real measurements of that county, so they were anonymised rather than re-pointed — 5.5%/21%/24.7% and 15.8 ug/m3 still stand, now attributed to 'one interior-west county'. Anonymising does not make the place unfindable, and is not meant to: the goal was to stop shipping one person's target as the project's built-in answer.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-003", "kind": "finding", "title": "Personal data is in every commit tree, and the window to rewrite closes at publication", "status": "open", "severity": "P0", "release_gate": "blocker", "effort": "medium", "lens": "personal-data", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-002", "fnd-011", "fnd-067", "fnd-068"], "evidence": ["the personal profile and .claude/settings.json both introduced in cfb6988, the initial commit", "both live at HEAD, so present in all 21 trees", "a history-wide grep for the county name, its FIPS and the settings symlink target returned 4, 3 and 1 hits", "history rewritten via git checkout --orphan: one root commit, b7ec275, with no parent", "reflog expired and git gc --prune=now run, dropping the unreachable objects locally", "the audit's own tracking file was scrubbed in the same commit — it had reproduced the county, the FIPS and the author's commit email verbatim as evidence", "REOPENED by cyc-release-audit-2 and reproduced directly: gh api repos/krimsonkla/refugia/contents/profiles/jason.json?ref=b38ccb8ae9 returns the personal profile with home_fips 41017", "the recorded post-rewrite root b7ec275 is not a valid object — git cat-file -t fails on it; the actual root is 9854732, the amend that followed"], "updated": "2026-09-07", "notes": "Deleting at HEAD leaves the blobs readable at 21 commit URLs forever. One author, one branch, no tags, no PR history, so rm -rf .git plus a single initial commit — or git filter-repo --invert-paths over those two files — is cheap now and impossible later. Do not rewrite the author identity: commit authorship under a public GitHub account is public by construction. Owner's decision; the reproducibility lens judged a rewrite unwarranted and that read is legitimate. FIXED: rewritten to a single root commit before the repo was ever public. The findings layer itself turned out to be the largest remaining leak — it quoted the profile's name field, the home FIPS and an email that appeared nowhere else in the tree. Scrubbed into the same root commit rather than a follow-up, since a fix committed on top would have left the leaking blob in the root and defeated the rewrite. Anything asserting the tree is clean must include docs/data in its grep; the early sweeps excluded it. REOPENED. The rewrite was local only. A force-push does not delete objects, GitHub still serves every pre-rewrite commit by SHA, and the activity API hands those SHAs to anyone with read access — so nothing has to be guessed. This was flagged at the time as a caveat and understated: force-push was called 'probably sufficient' and re-creating the repository 'belt-and-braces'. It is the other way round. See the two cycle-2 rows in `related`.", "finding_kind": "cycle-1", "verified_by": "", "batch": "publish-the-repo"}
{"id": "fnd-004", "kind": "finding", "title": "The private devenv layer made the repo unbuildable for everyone but its author", "status": "fixed", "severity": "P1", "release_gate": "blocker", "effort": "small", "lens": "reproducibility", "cycle": "cyc-release-audit", "refs": ["README.md#use", "CLAUDE.md#build"], "depends_on": [], "blocked_by": [], "related": ["fnd-019", "fnd-024", "fnd-049", "fnd-047"], "evidence": ["devenv.yaml:3 pins git+ssh://git@github.com/krimsonkla/devenv-layers", "gh repo view krimsonkla/devenv-layers -> \"visibility\":\"PRIVATE\"", "devenv.lock:55,59 pin the same ssh:// URL", "devenv.nix:3-7 hard-imports three paths from that input, so nix evaluation cannot proceed", "every runnable line in README.md:49-52 and CLAUDE.md:27-28 is prefixed 'devenv shell -- '", "devenv.yaml now declares one public input, github:cachix/git-hooks.nix", "devenv.lock went from 17 nodes to 6; no git+ssh URL remains", "devenv.nix configures languages.python directly against pkgs.python312", "devenv.config.toml deleted — every key in it was layer schema", "devenv shell -- prek run --all-files: 13 hooks pass"], "updated": "2026-09-07", "notes": "Add an Install section above README.md:46 — Python >=3.12, uv sync --all-groups, then uv run refugia fetch / uv run refugia rank --profile profiles/example.json — and relabel the devenv lines as a maintainer convenience. The escape route is verified: a fresh clone with uv sync --all-groups && uv run pytest gives 48 passed. Note python -m refugia.cli only works because devenv.nix:24 exports PYTHONPATH=$(pwd)/src, so the docs must give the uv run form. Soften CLAUDE.md:16-22 accordingly. FIXED: the layer is gone and the nix path now evaluates for anyone. The README's commands also moved from `python -m refugia.cli` to the `refugia` console script, since dropping the layer removed the PYTHONPATH export that was masking the fact that `python` is the bare interpreter. What remains is documentation, not access — see fnd-063.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-005", "kind": "finding", "title": "The generated page ships as an HTML fragment with no doctype", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "trivial", "lens": "security-quality", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-018"], "evidence": ["template.html:1 starts with <title>", "no <!doctype html>, <html lang>, charset or viewport in 3,152 lines", "builder.py:44-48 does a plain replace and write", "11+ non-ASCII characters including the sources string at template.html:2680", "ArtifactBuilder.as_document wraps the fragment at write time: doctype, html lang=en, utf-8 charset, viewport", "the template stays a fragment on purpose — a host that publishes it supplies its own document, and nesting a second <html> is worse than none", "5 tests written failing first, including that the style block stays in the head and a fragment with no style block still becomes a document", "rebuilt out/refugia.html (3.4 MB) and confirmed the doctype, the charset, <style> before </head> and the payload after it", "rendered in Chrome from a local server: page loads, map draws, no console errors"], "updated": "2026-09-07", "notes": "The template was authored against the Claude Artifact host, which supplies a head, so out/refugia.html renders in quirks mode with mojibake and desktop width on every phone. Prepend the four lines in builder.py, or to the template — the Artifact host tolerates a full document. Best value-per-line in the audit. FIXED in builder.py rather than the template, which is the finding's first suggestion and the right one: the template is a fragment because a publishing host wraps it, so only the file written to disk needs the document.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-006", "kind": "finding", "title": "The README documents neither the full CLI surface nor what a run costs", "status": "fixed", "severity": "P2", "release_gate": "important", "effort": "medium", "lens": "user-docs", "cycle": "cyc-release-audit", "refs": ["README.md#use"], "depends_on": [], "blocked_by": [], "related": ["fnd-007", "fnd-023", "fnd-025"], "evidence": ["cli.py:75-160", "cli.py:79 default universe is metro_micro = 1,906 counties; registry.py:22 offers 'all'", "a cold fetch leaves ~90 MB across ~6,860 files under data/cache/ and a ~1.4 MB data/dataset.json", "no getenv/environ/api_key anywhere in src/", "README has an option table for all five commands, generated by reading `--help` rather than the source", "every documented option and default cross-checked against the running tool: 10 of 10 match", "states the default universe is metro_micro = 1,906 counties and that changing it needs a refetch", "'What a fetch costs' uses measured numbers: 87 MB across 6,862 files, dataset.json 1.3 MB", "'Reading the output' explains that score is a percentile among survivors and cover is the share of weighted metrics present", "the ask section now names the Ollama prerequisite and the ~18 GB pull"], "updated": "2026-09-07", "notes": "One README pass covering: every option and default for fetch (--universe/--radius-km/--only), rank (--top/--explain), publish (--out) and ask (--model/--host); that the default universe is metro_micro, not all US counties, and changing it needs a refetch; what a cold fetch costs on disk and that it is resumable; that no API keys or accounts are needed for any source and that rank/publish are fully offline after fetch; that ask needs a separately installed Ollama (~18 GB) and is optional; and how to read the output — totals are 0-100 percentile positions among surviving places, so not comparable across profiles or filter settings, and cover (the page calls it 'Data', template.html:1999) is the share of weighted metrics present. FIXED. Two claims were corrected while writing rather than shipped: 'nothing is sent anywhere' for ask is only true by default, since --host will point at a remote Ollama, and the no-API-keys note landed beside Install rather than here.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-007", "kind": "finding", "title": "The profile schema — the tool's primary input — is documented nowhere", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "user-docs", "cycle": "cyc-release-audit", "refs": ["README.md#use"], "depends_on": [], "blocked_by": [], "related": ["fnd-002", "fnd-006"], "evidence": ["the only written statement of the vocabulary is the LLM prompt at ask/planner.py:25-30", "profile.py:69-75 rescales weights", "engine.py:76-78 resolves criteria fields", "normalizer.py:24-25 offers percentile|minmax", "21 metric keys: 10 adapter, 5 from cdc_places.py MEASURES, 6 from specs/", "README has a 'Writing a profile' section covering weights, criteria, normalization, min_coverage, home_fips and name", "`refugia metrics` added to cli.py, printing key/direction/category/unit from build_registry() so the list cannot drift from the code", "engine.py's unknown-key KeyError now points at `refugia metrics`", "4 tests for the command and 1 for the error, written failing first; suite 62 -> 67", "every metric key named in the README checked against the live registry — only the illustrative a/b placeholders are not real keys", "the documented state not_in filter executed against the real classes and confirmed"], "updated": "2026-09-07", "notes": "Add a 'Writing a profile' section: name; weights (relative, rescaled, never negative because direction already encodes good/bad); criteria as {field, comparison in min|max|in|not_in, value} where field is a metric key or a place attribute; normalization; min_coverage; optional home_fips. Add the 21-key metric table and a 'refugia metrics' command printing it from build_registry() so the table cannot drift. Cheapest partial: widen the KeyError at engine.py:40 to name the valid keys. This matters more once the personal profile is removed — the docs then carry the whole weight. FIXED. The command matters more than the prose: a written key list is a second source of truth that goes stale, and this one is printed from the registry. Documenting the schema also turned up a live trap — see fnd-013.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-008", "kind": "finding", "title": "No CI, and the documented lint gate is unreproducible off this machine", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "contributor-docs", "cycle": "cyc-release-audit", "refs": ["CLAUDE.md#build"], "depends_on": [], "blocked_by": [], "related": ["fnd-004", "fnd-014", "fnd-047", "fnd-069"], "evidence": ["no .github/ directory exists", "18 of 21 commits touched no test, including all five most recent, all of which edit template.html", ".pre-commit-config.yaml is a gitignored symlink to /nix/store/5zg1...", "prek is not in pyproject.toml", "CLAUDE.md:28 documents a command nobody else can run", "those hooks run ruff/black/pylint/prettier and zero pytest hooks", ".github/workflows/ci.yml on push to main and on every pull request", "checkout, setup-uv with cache, uv python install 3.12, uv sync --all-groups --locked, ruff check, black --check, pylint src/refugia, pytest", "all four commands verified passing locally before committing, and uv sync --locked confirms the lock matches pyproject", "deliberately the uv path, not devenv: a fork PR has no credential for anything private, and this makes CI fail when the contributor's path breaks", "first run green in 19s; after pinning to actions/checkout@v7 and astral-sh/setup-uv@v7, green in 22s with no deprecation annotation", "one failed run in between: setup-uv@v10 is a real release but not a resolvable major tag, which the tag list showed and the release list did not"], "updated": "2026-09-07", "notes": "Add .github/workflows/ci.yml on push and PR: checkout, astral-sh/setup-uv, uv sync --all-groups, then ruff check, black --check, pylint src, pytest. Deliberately not via devenv — a fork PR has no SSH credential for the private layer, and this way CI doubles as the guarantee the outsider path keeps working. FIXED. --locked is the quietly valuable part: it catches a pyproject change committed without a relock, which is the dependency mistake that reproduces everywhere except the machine that made it. Not covered: prettier, which would need node on the runner, and the page JavaScript, which nothing executes — that is fnd-013's parity test. Verified by watching the run, not by assuming the YAML was right. CONTRADICTED by cyc-release-audit-2: the CONTRIBUTING list omits the coverage floor CI enforces, and the pre-commit hooks are a superset on formatting and a subset on tests rather than the same checks.", "finding_kind": "cycle-1", "verified_by": "contradicted by cyc-release-audit-2", "batch": ""}
{"id": "fnd-009", "kind": "finding", "title": "Required upstream citations are missing, and non-metric sources are credited nowhere", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "data-licensing", "cycle": "cyc-release-audit", "refs": ["README.md#adding-a-metric"], "depends_on": [], "blocked_by": [], "related": ["fnd-001", "fnd-010", "fnd-019"], "evidence": ["template.html:2678-2679 derives the whole credit line from M.map(m => m.source)", "places/registry.py:14-19 Census centroids and CBSA delineation credited nowhere", "artifact/city_markers.py:11-18 and artifact/geometry.py:9 (us-atlas) credited nowhere", "specs/*.json:9 — five County Health Rankings specs", "zillow_home_value.py:41 emits the bare token \"Zillow ZHVI\"", "countyhealthrankings.org/terms-use: 'non-exclusive license to access and use ... for personal or non-profit purposes including educational, research, or public health'", "same page: 'may not be copied or otherwise used for commercial use (including without limitation, use to train AI models intended to be offered as commercial products) without CHR&R's express prior written consent'", "five of six specs are CHR&R: air_pollution, life_expectancy, poor_mental_health_days, poor_or_fair_health, social_associations", "zillowgroup.com/developers/api/public-data/real-estate-metrics: 'free for public use by consumers, media, analysts, academics and policymakers' and 'Proper and clear attribution of all data to Zillow is required'", "us-atlas is ISC-licensed, derived from Census cartographic boundary shapefiles; ISC requires the copyright notice on redistribution", "DATA_SOURCES.md written: one entry per endpoint across all ten publishers, each with the URL, what is taken, the terms, and whether the position is verified or inferred", "the CHR&R citation is carried verbatim in the page footer, with the non-commercial restriction stated", "verified rendered in the built page, not just present in the template source", "LANDFIRE and Wildfire Risk to Communities are explicitly labelled inferred rather than asserted"], "updated": "2026-09-07", "notes": "Write DATA_SOURCES.md, one row per endpoint (URL, publisher, what is taken, terms, required citation) for all ten. Two carry real obligations: County Health Rankings requires the citation 'University of Wisconsin Population Health Institute. County Health Rankings & Roadmaps. www.countyhealthrankings.org' and restricts use to personal and non-profit — verified verbatim from their terms page — and Zillow requires proper and clear attribution. Add the CHR&R citation to the artifact footer too. Anything that is not a registered metric structurally cannot appear in the current footer. VERIFIED 2026-09-07: the CHR&R citation and the non-commercial restriction are confirmed verbatim from their terms page, as is Zillow's attribution requirement. Add the ISC notice for us-atlas alongside them — the artifact embeds projected paths derived from it, which is redistribution. FIXED. The citations live in the page footer rather than only in the repository, because a published page is the thing that gets shared and the obligation travels with it. DATA_SOURCES.md closes with the point fnd-019 makes structurally: a Metric has no citation or terms field, so this file is kept in step by hand and will drift until it does.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-010", "kind": "finding", "title": "Zillow and CHR&R terms verified; two positions remain inferred", "status": "fixed", "severity": "P2", "release_gate": "important", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-009"], "evidence": ["Zillow: verified via zillowgroup.com/developers/api/public-data/real-estate-metrics — free public use with required attribution. The stricter no-derivative-works language applies to the Public Records Data API, not the research CSVs", "CHR&R: verified verbatim at countyhealthrankings.org/terms-use", "NASA POWER: 'no restrictions on the use, access, and/or download'; citation requested", "wildfirerisk.org: USDA Forest Service with Headwaters Economics and Pyrologix; no licence statement published — federal public domain still inferred, not established", "LANDFIRE (USGS/USFS) public domain not independently verified"], "updated": "2026-09-07", "notes": "Superseded by fnd-056 for the redistribution question. Two positions stay inferred and should be labelled as such in DATA_SOURCES.md rather than asserted: wildfirerisk.org publishes no licence and has non-federal partners, so public domain does not follow automatically from USDA funding; LANDFIRE was not independently checked.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-011", "kind": "finding", "title": "A tracked nix-store symlink dangles for every cloner and leaks the one absolute path", "status": "fixed", "severity": "P2", "release_gate": "important", "effort": "trivial", "lens": "personal-data", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-002", "fnd-003", "fnd-048"], "evidence": [".claude/settings.json tracked as mode 120000, blob 67efc6a", "its entire 106-byte content is a /nix/store path whose filename embedded the author's home directory", "git rm --cached .claude/settings.json", ".gitignore now carries .claude/ and .DS_Store"], "updated": "2026-09-07", "notes": "git rm --cached .claude/settings.json and add .claude/ to .gitignore — one line, which also covers .claude/settings.local.json, currently ignored only by the global ~/.config/git/ignore. Add .DS_Store in the same edit. Ride this in the scrub commit and include the path in any history rewrite. See fnd-048 for what this finding is NOT. FIXED: untracked and ignored. The blob is still in history — that is fnd-003's decision, not this one's.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-012", "kind": "finding", "title": "Six shipped metrics and the composition root have no test at all", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "medium", "lens": "tests", "cycle": "cyc-release-audit", "refs": ["CLAUDE.md#adding-a-metric"], "depends_on": [], "blocked_by": [], "related": ["fnd-013", "fnd-055"], "evidence": ["grep -rn specs tests/ returns nothing", "workspace.py at 0% coverage", "cli.py at 0% coverage", "statistics.py percentile_rank lines 16 and 18 (count == 0, count == 1) uncovered", "tests/test_specs.py: every shipped spec loads through the validator, is named after the metric it declares, and a spec that cites a source must link its terms", "the registry test asserts unique keys and that all 21 metrics register — the composition root's actual job", "statistics.py reaches 100%: the count == 0 and count == 1 branches now have their own tests", "an autouse conftest fixture makes httpx raise, so no test can quietly go online; 146 tests pass under it, which independently confirms the 'offline by construction' claim in CONTRIBUTING", "mutation-checked: downgrading a spec URL to http failed 4 tests, citing a source without terms failed 1, and a test that calls httpx.get fails with the guard's message", "suite 117 -> 146"], "updated": "2026-09-07", "notes": "In priority order: tests/test_specs.py parametrised over sorted(Path('specs').glob('*.json')) calling DeclarativeSource.from_file on each, so a typo stops surfacing as a traceback on a user's first fetch; tests/test_workspace.py asserting build_registry() succeeds with unique keys, since that is where a duplicate key raises in front of the user; tests/test_cli.py via typer.testing.CliRunner for rank and publish; tests/test_statistics.py with four vectors on percentile_rank, which double as the reference vectors for a JS parity test. Add an autouse conftest fixture patching httpx.Client.send to raise so new adapter tests cannot quietly go online. FIXED against its stated remedy, all five parts. Two of them had aged: workspace.py was at 0% when the row was written and is now 95%, and cli.py was 0% and is 59%, both covered incidentally by later work. What the row did not ask for, and this does not deliver, is adapter coverage — the total is 60% and the remainder is dominated by the network adapters. That is the separate known gap the README already states; see fnd-065 rather than reading this row as closing it.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-013", "kind": "finding", "title": "The Python and JavaScript scorers disagree on zero-weighted and negative-weighted metrics", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "medium", "lens": "tests", "cycle": "cyc-release-audit", "refs": ["CLAUDE.md#gotchas", "README.md#known-gaps"], "depends_on": [], "blocked_by": [], "related": ["fnd-012", "fnd-052", "fnd-082"], "evidence": ["template.html:1259 filters w > 0 before scoring and before the cover denominator", "profile.py:75 keeps all keys", "engine.py:99 missing.append(key) then inflates the coverage denominator", "reproduced: weights {j:1.0, p:0.0} with no data for p empties the ranking under min_coverage=0.8; {j:1.0} returns both places", "the JS filter also silently drops negative weights, which Python's abs() treats as penalties", "re-confirmed while documenting the schema: weights {j:1.0} ranks 2 places, {j:1.0, p:0.0} ranks 0, because the zero-weighted metric still counts toward min_coverage", "README now warns to omit a metric rather than weight it 0, which is a workaround and not a fix", "tests/conformance/vectors.json: 12 shared cases run by both implementations", "the JavaScript half is now a pure kernel between 'scoring kernel: BEGIN/END' markers in template.html, lifted verbatim by run_vectors.mjs rather than copied", "tests/test_scoring_conformance.py runs the vectors through the Python engine and shells out to node for the page's; it fails rather than skips when CI is set", "the suite caught the divergence on its first run: the zero-weight and negative-weight cases failed on the Python side before anything was fixed", "Profile.normalized_weights now drops weights <= 0, which is what the page always did; both sides then agreed on all 12", "mutation-checked in both directions: removing the lower_better inversion in the page failed 2 vectors, breaking tie-averaging in Python failed 2", "page rebuilt and driven in a browser after the refactor — renders, toggling a metric re-ranks, no console errors"], "updated": "2026-09-07", "notes": "A real disagreement in exactly the place CLAUDE.md says nothing enforces agreement. Not reachable from any shipped path — no zero or negative weight in any profile, and the page has no profile export — but it is the divergence the gotcha warns about. Pick one behaviour, pin it in profile.py:73-75, add both cases to tests/test_engine.py, and correct README.md:151. A real parity test needs node in devenv.nix; confirm before promising one. Reachable by a user after all: the original row said no shipped path reaches it, but a hand-written profile that zero-weights a metric rather than omitting it does, and empties the ranking with no explanation. The README warns; the divergence is still there. FIXED, by pinning rather than by deleting one implementation. The duplication is forced: the published page re-ranks when a slider moves and has no server to ask, so scoring has to exist in the browser. What was not forced was the two drifting, and nothing detected it. Reading both closely also showed they are not parallel implementations at all — they share about forty lines of arithmetic and deliberately differ on filters, on home-as-reference-point, and on which metrics get normalised. That is why the vectors cover only the shared kernel, and why fnd-013's own divergence sat in the surrounding logic where nobody was looking. The zero-weight question was resolved in the page's favour: a weight of zero is a metric not asked for, so it leaves the coverage denominator too. README, CLAUDE.md and CONTRIBUTING all said nothing enforced agreement; all three were corrected. CONTRADICTED by cyc-release-audit-2. The vectors pin the shared kernel and the page ignores `criteria` and `normalization` entirely, which the vectors cannot see because they only ever pass weights and min_coverage. For the shipped example profile the CLI and the page rank differently — 1101 survivors against 1773, a different top county, and no shared place scoring the same. Reproduced.", "finding_kind": "cycle-1", "verified_by": "contradicted by cyc-release-audit-2", "batch": ""}
{"id": "fnd-014", "kind": "finding", "title": "No CONTRIBUTING.md, so the setup, the checks and the commit convention are all folklore", "status": "fixed", "severity": "P2", "release_gate": "important", "effort": "medium", "lens": "contributor-docs", "cycle": "cyc-release-audit", "refs": ["CLAUDE.md#build", "CLAUDE.md#adding-a-metric"], "depends_on": [], "blocked_by": [], "related": ["fnd-004", "fnd-008"], "evidence": ["git log shows 12 feat, 8 fix, 1 refactor — always scoped, lowercase imperative", "the only URLs in tests/ are https://example.invalid/...", "specs/ is the zero-code path to a new metric", "CONTRIBUTING.md covers setup, the checks, tests, adding a metric, the conventions that reject a PR, commits and what not to commit", "its check list is byte-identical to what ci.yml runs, compared programmatically rather than by eye", "all four commands verified passing before the claim shipped", "commit types taken from scripts/commit-msg-convention.sh, which enforces them, rather than from the observed history", "linked from the README"], "updated": "2026-09-07", "notes": "Cover: setup via uv sync --all-groups with devenv noted as maintainer-only; that the suite is offline and needs no keys; the checks that actually run (ruff check, ruff format --check, black --check, pylint src, pytest); the observed commit convention; a branch and PR decision; a pointer to specs/ as the zero-code way to add a metric; and one line explaining the dangling .pre-commit-config.yaml symlink. FIXED. Two drafted claims were corrected against the code: the suite avoids the network three different ways rather than only by pre-seeding the cache, and the single-file coverage figure is 20% against the suite's 59% — both now stated as measured. It points at CLAUDE.md for the full convention set rather than restating it, since a second copy is the thing that drifts.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-015", "kind": "finding", "title": "'Never cache an empty result' is enforced in one adapter out of six", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "security-quality", "cycle": "cyc-release-audit", "refs": ["CLAUDE.md#gotchas"], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["enforced only at landfire_vegetation.py:196 (if counts:)", "unguarded: store/cache.py:56-58, cdc_places.py:128, nasa_power_climate.py:183-187, declarative.py:119, city_profile.py:105", "Cache.write returns None rather than storing an empty payload — one guard covering the zero-byte case at every call site", "cdc_places, nasa_power_climate and city_profile each guard their own write, because an empty parsed result serialises to a valid {} or [] that Cache.write cannot see", "declarative's xlsx write needs no guard of its own: its payload comes from fetch_url, which no longer caches an empty body", "5 tests in tests/test_cache.py, written failing first, including that an empty body is re-requested on the next run rather than served from disk", "no caller binds write()'s return value, so the signature change breaks nothing"], "updated": "2026-09-07", "notes": "cache.py:56-58 persists any 2xx body, including zero bytes and the 200-shaped error pages CLAUDE.md documents for LANDFIRE — which is exactly the failure the rule exists to prevent, and a cached failure is indistinguishable from data and survives every later run. Five one-line guards plus a test asserting an empty body leaves no cache file. FIXED, and split in two rather than the five identical guards the row proposed. Zero bytes is a property of the store and belongs in Cache; an empty parsed result is domain-specific and cannot be seen from there, since `{}` is a perfectly good JSON document. CLAUDE.md now says which half lives where.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-016", "kind": "finding", "title": "No User-Agent on any request, and a cold fetch fires ~6,600 unthrottled POSTs at one USGS endpoint", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "data-licensing", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-009", "fnd-075", "fnd-076"], "evidence": ["grep for headers across src/ returns nothing", "outbound calls at cache.py:56, landfire_vegetation.py:232, cdc_places.py:104, nasa_power_climate.py:195, city_profile.py:131", "1,825 county + 4,797 city POSTs counted from the cache, from two 8-worker pools with no inter-request delay", "time.sleep at landfire_vegetation.py:218 only runs in the exception handler", "landfire_vegetation.py:203-207 docstring records losing a full run of nineteen hundred requests", "USER_AGENT defined once in refugia/__init__.py as refugia/<version> (+repo url), sent from all five outbound call sites", "Throttle added: a lock-held minimum interval between request starts, shared across the worker pool, with clock and sleep injected so it is tested without spending wall time", "Sampling gains min_interval, default 0.05s — a 20/s ceiling on a pool that eight workers could otherwise drive far higher", "5 unit tests for Throttle and 6 for the request-identity check; suite 67 -> 83", "tests/test_outbound_requests.py walks the AST of every module and fails any httpx call without the shared USER_AGENT — mutation-checked by removing the header and confirming it fails"], "updated": "2026-09-07", "notes": "A public repo multiplies request volume across every fork, so this stops being theoretical at publication. Define one User-Agent constant and pass it at all five call sites; add a rate field to Sampling and a sleep before the POST. FIXED. The throttle is a ceiling rather than a slowdown: at eight workers and a second or two per request the real rate is already well under 20/s, so it costs nothing on a healthy run and bounds the pathological one. The structural test is the part that matters for a public repo — the risk was never that these five adapters lose their header, it is that a contributed sixth never gets one, and no runtime test of the first five can fail for that. ask/planner.py is exempt and says why: it talks to an Ollama on the reader's own machine. CONTRADICTED by cyc-release-audit-2. Two claims here were wrong. The throttle was described as a ceiling on the grounds that a request takes a second or two; measured over the cache it is 15.04 req/s sustained with a median gap of 0.0524s against the 0.05s floor — the pool sits on the floor, so the floor IS the rate. And the structural guard recognises only literal httpx.get/httpx.post: a probe adapter using httpx.Client().get() passed unflagged.", "finding_kind": "cycle-1", "verified_by": "contradicted by cyc-release-audit-2", "batch": ""}
{"id": "fnd-017", "kind": "finding", "title": "The tool ranks counties on health outcomes with no statement of what the numbers are not", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "trivial", "lens": "critique", "cycle": "cyc-release-audit", "refs": ["README.md#known-gaps"], "depends_on": [], "blocked_by": [], "related": ["fnd-005", "fnd-018"], "evidence": ["grep -rniE 'disclaimer|no warranty|not advice|as is' across README.md, CLAUDE.md and src/ returns zero hits", "cdc_places.py:53 already describes these as model-based prevalence estimates", "ranked metrics include depression, asthma, life expectancy and mental distress", "CAVEAT appended to the artifact footer so it travels with a shared page", "a blockquote near the top of README.md says the same thing", "verified rendered in the built page"], "updated": "2026-09-07", "notes": "One paragraph near the top of README.md and one sentence appended to the artifact footer at template.html:2680 so it travels with the shared page: county-wide, many modelled rather than measured, ranked against whoever's weights built the page, a way to narrow a list rather than advice. Cheap, and it is the difference between reading as careful and reading as careless about real places where real people live. FIXED. Worded to say what the numbers are rather than to disclaim liability: county-wide, several modelled, ranked against weights the reader chose, a way to narrow a list rather than a verdict on anyone living there.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-018", "kind": "finding", "title": "Two README claims do not match the code", "status": "fixed", "severity": "P2", "release_gate": "important", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit", "refs": ["README.md#asking-questions", "README.md#adding-a-metric"], "depends_on": [], "blocked_by": [], "related": ["fnd-005", "fnd-054"], "evidence": ["README.md:110-111 says the artifact declares the 'sample' capability; builder.py:30-49 declares nothing, it substitutes JSON and writes a file", "the page only probes the host at template.html:3151, :2923-2925, :2460", "README.md:89-91 calls tier-2 specs safe for a model to author; declarative.py:47-73 _validate never inspects url", "declarative.py:26-27 states the honest version", "the ask section now says what happens: nothing declared at build time, the page probes window.claude.use at runtime and requests the 'sample' capability, and the panel stays hidden when that is declined or absent", "the spec tier is described as bounded rather than safe: nothing executes, url must be https, displayed fields may not contain markup, a wrong spec shows up as poor coverage — but it is still a network request, so read the URL", "a third false claim found in the same section and fixed: 'the first question asks the viewer for consent' — the page has no consent step at all, and grep for consent/permission in template.html finds only an unrelated CSS comment", "every rewritten claim checked against the code: the window.claude.use probe, builder.py substituting only, both _validate guards, the coverage gate, and all four named ask functions present in the page"], "updated": "2026-09-07", "notes": "Opening out/refugia.html from disk silently shows no Ask panel, which the README does not prepare anyone for. And a spec is a network request in data clothing — the module docstring is more accurate than the README. FIXED, and the row was wrong in one direction too: the page does request the sample capability, just at runtime via claude.use('sample') rather than at build time, so 'declares nothing' needed saying precisely rather than flatly. Two drafts were corrected mid-fix — first that nothing is requested at all, then that nothing is sent 'until a question is typed', which missed the Summarise button as a second user-initiated path. Both paths verified as click or submit handlers before the claim shipped.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-019", "kind": "finding", "title": "Nothing records when the data was fetched, so a stale page is indistinguishable from a fresh one", "status": "fixed", "severity": "P2", "release_gate": "important", "effort": "small", "lens": "critique", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-004", "fnd-009", "fnd-021"], "evidence": ["Dataset.to_dict() serialises exactly places/metrics/values", "Metric has seven fields, none a date", "cache.py:52-53 makes fetch cache-first", "Dataset gains built_at and oldest_response, both optional so a dataset saved before them still loads", "Cache.oldest_entry() dates the oldest stored response — the number that actually distinguishes a stale build, since a cache-first run stamps today onto year-old figures", "Metric gains citation and terms_url, defaulting empty; a spec may declare both", "5 County Health Rankings specs and the Zillow adapter now carry their required citation", "the page footer builds its citation list from the metrics, deduped, instead of a hardcoded string; only the non-metric sources are still named by hand", "7 tests written failing first; suite 104 -> 111", "verified by rebuilding: dataset stamped built_at 2026-09-07 / oldest_response 2026-09-05, and the footer rendered both citations once each plus the provenance line, with no console errors"], "updated": "2026-09-07", "notes": "Add fetched_at to Dataset, set it in cli.py fetch, render it beside the sources line. Pairs with adding citation and terms_url fields to Metric — today attribution structurally cannot travel with a metric, which is why every attribution fix is prose that rots. FIXED, with one deliberate departure from the remedy. A single fetched_at would not have solved the stated problem: fetch is cache-first, so stamping the run time onto a build made entirely of cached responses launders stale data as fresh, which is worse than saying nothing. Two dates are recorded instead — when the dataset was assembled, and how old the oldest response behind it is — because those answer different questions. The Metric half is the structural fix fnd-009 was missing: a source added later arrives carrying its own obligation rather than depending on someone remembering to edit DATA_SOURCES.md, whose closing paragraph said the opposite and has been corrected.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-020", "kind": "finding", "title": "One rotted upstream URL kills every source after it, and a missing dataset prints a traceback", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "critique", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-019", "fnd-078"], "evidence": ["nine upstreams pinned to dated paths: analytic_data2025_v2.csv x5 specs, wrc_download_20260415.xlsx, epa_air_quality.py:14 YEARS = (2019..2025), LF2024_*, CenPop2020/list1_2023.xlsx, sub-est2024.csv/2024_Gaz_place_national.zip", "the per-source loop at cli.py:28-36 is unguarded", "cli.py:117,142,160 — rank/publish/ask before fetch raise a raw FileNotFoundError for data/dataset.json", "_collect wraps each source, records (name, reason), reports it in red and continues; returns (values, failures)", "_report_failures repeats the list after the coverage table, so a failure an hour into a run is the last thing printed", "_load_dataset guards rank, publish and ask: 'no dataset at <path>, run `refugia fetch` first' and exit 1 instead of FileNotFoundError", "7 tests written failing first, including that existing values survive a failure and that all sources failing is still a report", "verified end to end against the real CLI: a spec pointed at a 404 left the run intact, saved the dataset, printed the failure with the dead URL, and air_pollution kept its previous 95.4% coverage"], "updated": "2026-09-07", "notes": "Wrap the per-source loop, report the failure in red and continue, matching the 'partial returns are legitimate' doctrine in metrics/source.py. Separately, guard the three dataset loads with a 'run refugia fetch first' message — one helper. Upstream drift is a certainty on a published project, not a risk. FIXED. The except is deliberately broad and says so: an adapter can fail in as many ways as the format it parses, and each of them should cost one metric rather than the run. The end-of-run repeat matters as much as the guard — the same reasoning that produced _report_coverage, since a red line three thousand lines up the scrollback is not a report. Verified by actually pointing a spec at a dead URL rather than only by stub, which is also what confirmed the 'keeps whatever a previous fetch saved' claim the message makes. AMENDED by cyc-release-audit-2: fetch survives a source that RAISES. A source returning an empty mapping without raising still replaces the previous values wholesale, and the saved dataset is stamped as a fresh build either way.", "finding_kind": "cycle-1", "verified_by": "contradicted by cyc-release-audit-2", "batch": ""}
{"id": "fnd-021", "kind": "finding", "title": "No escape helper anywhere in a page built from data the project will start accepting by PR", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "medium", "lens": "security-quality", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-018", "fnd-053", "fnd-079"], "evidence": ["18 innerHTML assignments in template.html and no escape helper", "unescaped: :1405 ${m.label}, :1411 ${m.description}, :1597 ${m.label}, :2016 ${c.l}, :2046 ${countyLabel(r.p)} and ${r.p.cbsa_name}, :3079 ${item.name}", "attribute-position href values built from p.name/p.lat/p.lon at :2227-2258", "builder.py:47 escapes only </ -> <\\/, which protects the JSON payload and nothing downstream", "declarative.py _validate never inspects content", "esc() added at the top of the page script, escaping & < > \" ' so one helper serves both a quoted attribute and a text node", "30 esc() call sites; every data-bearing interpolation into markup now passes through it", "audited all 110 remaining interpolations into markup: numbers, booleans, computed CSS classes, and three (paths, borders, markers) that are intentional HTML", "outbound hrefs escaped at the render site; the Zillow link already used encodeURIComponent", "node --check over the extracted 2,058-line script: syntax OK", "esc() unit-checked against img/onerror, attribute-quote breakout in both quote styles, </script> breakout, bare &, a non-ASCII county name, and null/undefined/0 — 9 cases, all pass"], "updated": "2026-09-07", "notes": "No live hole today — all six specs are the owner's and place names come from Census files — so this is not a release blocker. It becomes one the moment the first outside spec PR merges. Add one esc() helper, wrap those sites, encodeURIComponent the hrefs, and have _validate reject < in label/description/source. FIXED. Deliberately left unescaped: `label` and `verdict` are HTML fragments this code builds and whose own data is escaped where it enters them; `grain` and `word` are computed literals. The one residual path is fmt() output carrying a metric unit into markup, which is closed at the other end by fnd-037 — unit is now among the fields a spec may not put markup in. CONTRADICTED by cyc-release-audit-2: six further sites interpolate unescaped, including a spec-controlled `category` into three attributes and `m.key` into a data attribute. 'Every data-bearing interpolation' was not true.", "finding_kind": "cycle-1", "verified_by": "contradicted by cyc-release-audit-2", "batch": ""}
{"id": "fnd-022", "kind": "finding", "title": "The page's own interaction model is undocumented", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "user-docs", "cycle": "cyc-release-audit", "refs": ["README.md#use"], "depends_on": [], "blocked_by": [], "related": ["fnd-006"], "evidence": ["template.html localStorage key refugia.setups.v1", "README gains 'Using the page': live re-ranking, weights versus requirements, the map's metric selector, the county and city panels, the keyboard route, and saved setups", "the localStorage key was read from the source rather than recalled: refugia.setups.v1", "states that setups are per-browser and do not travel with a shared page, so a recipient sees the weights it was published with"], "updated": "2026-09-07", "notes": "A 'Using the page' section: sliders re-rank live, filters drop places, the map selector picks the shaded metric, clicking a county opens the city panel, and setups live in localStorage under refugia.setups.v1 so they are per-browser and do not travel with a shared page. FIXED, and written after fnd-042 rather than before, so the keyboard route it documents is the one that exists.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-023", "kind": "finding", "title": "A data-heavy visual project with no picture in the README", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "user-docs", "cycle": "cyc-release-audit", "refs": ["README.md"], "depends_on": [], "blocked_by": [], "related": ["fnd-006", "fnd-042"], "evidence": ["docs/screenshot-map.jpg and docs/screenshot-compare.jpg, both captured from a page built with profiles/example.json and confirmed not gitignored", "the README leads with them rather than with the CLI", "a trimmed `rank --top 3 --explain` block added, using real output rather than an invented example"], "updated": "2026-09-07", "notes": "Add docs/screenshot-page.png built from profiles/example.json (confirm the path is not gitignored), plus a trimmed rank --top 10 --explain block. A ranked-map tool that shows nothing on its landing page loses readers before the install section. FIXED. The screenshots landed while restructuring the README to lead with the page; the worked `--explain` block the row also asked for was the only part outstanding and is now in.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-024", "kind": "finding", "title": "A published page embeds the profile it was built from", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit", "refs": ["README.md#use"], "depends_on": [], "blocked_by": [], "related": ["fnd-002"], "evidence": ["builder.py:41", "README's sharing paragraph now says the profile travels with the page, home county included", "`publish` names the home county explicitly when one is set, and stays silent when it is not"], "updated": "2026-09-07", "notes": "Name and home_fips included. Not a repo leak — out/ is gitignored — but a sharing caveat the README should state, especially once users are told to write their own profiles. FIXED alongside fnd-056, which is the same caveat at the moment it matters rather than in a document read beforehand.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-025", "kind": "finding", "title": "One README sentence parses backwards", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit", "refs": ["README.md#known-gaps"], "depends_on": [], "blocked_by": [], "related": ["fnd-006", "fnd-052"], "evidence": ["README.md:152", "README:358 still read 'Network adapters are thinly tested; the scoring core is not' — it had survived the earlier known-gaps edit, which changed the adjacent scoring bullet only", "replaced with the measured position: scoring core, store and declarative tier above 92%, adapters thin, and the suite refuses a real request"], "updated": "2026-09-07", "notes": "'...the scoring core is not' understates coverage in the pessimistic direction — the scoring core is the best-tested code in the repo. FIXED. Worth recording how it survived: an earlier commit rewrote the bullet directly above it and I recorded the sentence as handled without re-reading it. By the time it was found it was wrong twice over — the scoring core is now the best-tested code in the repo, and fnd-065 had just been filed saying the adapters are thin.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-026", "kind": "finding", "title": "No project status or scope note", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "critique", "cycle": "cyc-release-audit", "refs": ["README.md"], "depends_on": [], "blocked_by": [], "related": ["fnd-029"], "evidence": ["README's opening now states: personal project, US counties only, CONUS-only map, pre-1.0 with the formats that may move, issues welcome, no support promise"], "updated": "2026-09-07", "notes": "US counties only, CONUS-only map, personal project, plus an issues pointer. Determines how every later scope question gets answered. FIXED. Placed in the opening rather than at the bottom, because it is the frame every later scope question gets answered against and a reader who gets it late has already formed a different expectation.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-027", "kind": "finding", "title": "The shared page contacts Google Fonts from every viewer's browser", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "critique", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-017"], "evidence": ["template.html:2-7 loads Fraunces, IBM Plex Mono and Source Sans 3", "README says the page loads typefaces from Google Fonts, that every viewer's browser therefore contacts fonts.googleapis.com, and that it falls back to system faces offline"], "updated": "2026-09-07", "notes": "Either disclose it in one README line or drop to a system font stack. An offline viewer already gets fallback fonts today. FIXED by disclosure rather than removal, which was the recommendation: the typography does real work in this page, and the request carries nothing about the viewer or the data. Stated where sharing is discussed, since the viewer is the person it affects.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-028", "kind": "finding", "title": "CLAUDE.md becomes public as-is and carries a private URL", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "contributor-docs", "cycle": "cyc-release-audit", "refs": ["CLAUDE.md"], "depends_on": [], "blocked_by": [], "related": ["fnd-004", "fnd-014"], "evidence": ["CLAUDE.md:13 names the private layer repo", "CLAUDE.md:16-22 forbids the only path outsiders have", "CLAUDE.md no longer names the private layer repo, and the 'never use host tooling' section is now 'Two ways in, and uv is the one everyone has'", "verified: grep for devenv-layers and 'never use host tooling' in CLAUDE.md returns nothing"], "updated": "2026-09-07", "notes": "Optionally lift ARCHITECTURE.md from CLAUDE.md:31-54,61-73 as a human-facing entry point. Fix :13 and :22 regardless — they are wrong for a public reader whether or not the file is split. FIXED — both mandatory halves were closed by the devenv-layer removal, and the row simply never got updated. The ARCHITECTURE.md split was explicitly optional and is not done: CLAUDE.md reads acceptably to a public audience now, and a second copy of the architecture is the kind of thing that drifts.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-029", "kind": "finding", "title": "No CHANGELOG, tag or stability statement", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "contributor-docs", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-026"], "evidence": ["CHANGELOG.md with a 0.1.0 entry and an explicit pre-1.0 statement naming the two formats users may depend on", "the README repeats the pre-1.0 position where a reader will meet it first"], "updated": "2026-09-07", "notes": "CHANGELOG.md, a v0.1.0 tag, and a 'pre-1.0, formats may change' sentence. The dataset JSON and profile JSON are both formats users will depend on. FIXED. The stability sentence is the part that matters: the profile JSON and the saved dataset are both formats somebody could build on, and saying they may move is cheaper than moving them silently.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-030", "kind": "finding", "title": "No SECURITY.md and no stated trust boundary", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "contributor-docs", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-021"], "evidence": ["SECURITY.md states the four real boundaries: a spec is a network request in data clothing, every upstream is untrusted, a published page redistributes what it embeds, and `ask` talks to a model that --host can move", "it also says what is not a vulnerability — an upstream moving a file is a data-quality issue the tool already survives", "linked from README and CONTRIBUTING", "private vulnerability reporting cannot be enabled while the repository is private: the API call is refused and security_and_analysis reads null. Added to fnd-033's post-flip list"], "updated": "2026-09-07", "notes": "About ten lines plus GitHub private vulnerability reporting. State the boundary explicitly: specs are reviewed input, fetched data is untrusted. FIXED. The trust boundary was worth writing down precisely because most of what a reader would look for is absent — no server, no accounts, no credentials — and saying that plainly is what makes the four things that do matter legible.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-031", "kind": "finding", "title": "No issue or PR templates and no code of conduct", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "contributor-docs", "cycle": "cyc-release-audit", "refs": ["CLAUDE.md#local-conventions"], "depends_on": [], "blocked_by": [], "related": ["fnd-014"], "evidence": ["issue forms for a new metric source and for a bug, both YAML and both validating under check-yaml", "the metric-source form requires coverage and requires saying whether the terms were read or inferred", "the bug form asks for the coverage table and for the page's built/oldest-response line, because most surprising rankings are a thin metric rather than a scoring bug", "PR template with sectioned checks: both scorers plus a conformance vector, coverage and attribution for a new source, esc() for the page, and the three architecture rules", ".github/dependabot.yml for github-actions and uv, monthly and grouped", "config.yml routes a vulnerability to private reporting rather than to an issue"], "updated": "2026-09-07", "notes": "A new-metric-source issue template, a bug template asking for the fetch coverage table, a PR template checking both scorers were updated / coverage >=75% / no 'if metric.key ==' outside a source, CODE_OF_CONDUCT.md, and .github/dependabot.yml. FIXED. The dependabot github-actions entry has already earned itself: both actions in ci.yml were pinned to majors that had moved onto a deprecated Node runtime, and nothing but an annotation on a green build said so. The forms ask for the two things that actually decide a report — coverage, and whether the terms were read or guessed — rather than for a general description.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-032", "kind": "finding", "title": "The GitHub repo carries no description, topics or packaging metadata", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "critique", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-001", "fnd-055"], "evidence": ["pyproject.toml:4 has a usable one-liner already", "pyproject gains authors, keywords, classifiers and [project.urls]", "the license classifier is deliberately omitted: it conflicts with the PEP 639 license expression already declared"], "updated": "2026-09-07", "notes": "Set the description and topics; fill out pyproject.toml with readme, authors, keywords, classifiers and [project.urls]. FIXED in the package metadata. The GitHub description and topics are set separately, on the repository rather than in the tree.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-033", "kind": "finding", "title": "Branch protection and secret scanning are impossible while the repo is private", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "critique", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-008"], "evidence": ["branch protection API returns 403 'upgrade or make public'", "private vulnerability reporting confirmed unavailable while private: security_and_analysis is null and enabling it is refused", "SECURITY.md and the issue-template contact link both point at the advisory flow, so both start working at the flip"], "updated": "2026-09-07", "notes": "At the flip: protect main requiring the CI check, and enable secret-scanning push protection and Dependabot alerts. All free on public repos. This is a post-flip step, not a pre-flip one. Now also covers private vulnerability reporting, which SECURITY.md tells reporters to use and which cannot be turned on until the repository is public. Checked rather than assumed.", "finding_kind": "cycle-1", "verified_by": "", "batch": "publish-the-repo"}
{"id": "fnd-034", "kind": "finding", "title": "GitHub Pages would beat a screenshot, but only after the doctype fix", "status": "declined", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "critique", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-005", "fnd-023"], "evidence": ["a hosted page needs data/dataset.json, which needs a cold fetch of 6,862 files and ~6,600 POSTs to one USGS endpoint — not something to run on a schedule from CI", "the workable form was committing the 1.3 MB dataset and building the 3.3 MB page in a workflow: about an hour", "declined by the owner as not worth doing"], "updated": "2026-09-07", "notes": "A page built from profiles/example.json and hosted on Pages demonstrates the whole tool including home_fips highlighting. Sequenced after fnd-005 — publishing a quirks-mode fragment as the demo would be worse than no demo. Unblocked as of fnd-056: the credits, the ISC notice and the provenance line are all in the footer now, so a Pages demo would redistribute with its attribution intact. Still a decision rather than a defect. WILL NOT DO — the owner's call, recorded so it is not raised again as an oversight. The technical path was clear and cheap: commit the dataset rather than fetch in CI, deploy through the Actions artifact route since docs/ is taken by this findings layer. What it would have cost beyond that hour is ongoing: a demo is a claim about current data, so it needs re-fetching when an upstream publishes a new year, and hosting it makes the owner rather than a user the party redistributing County Health Rankings values. The README's two screenshots already carry the 'what does this look like' job that the demo was mostly for.", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-035", "kind": "finding", "title": "Coverage reporting is misleading on a partial run", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-012"], "evidence": ["--cov lives in pyproject.toml addopts, so pytest tests/test_engine.py prints TOTAL 21%", "--cov removed from addopts, so a single-file run no longer prints a project-wide total measured from one file", "[tool.coverage.run] branch = true added; branch coverage moves the real number from 60% to 54.58%", "CI runs pytest --cov --cov-fail-under=54, verified to fail at a higher threshold and pass at that one"], "updated": "2026-09-07", "notes": "Move --cov out of addopts first, then add a --cov-fail-under ratchet and [tool.coverage.run] branch = true. FIXED. The floor is 54 rather than the 60 I first wrote, because enabling branch coverage lowered the true figure and a ratchet has to sit below where you actually are. Measured before choosing it.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-036", "kind": "finding", "title": "Warnings are not errors, and the slate is currently clean", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["48 passed with no warnings", "filterwarnings = [\"error\"] in pyproject.toml; 146 tests pass under it"], "updated": "2026-09-07", "notes": "filterwarnings = [\"error\"] costs nothing today and never gets cheaper than the moment the slate is clean. FIXED while the slate was clean, which is the only moment it costs nothing.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-037", "kind": "finding", "title": "A spec's url is never checked for scheme", "status": "fixed", "severity": "P2", "release_gate": "important", "effort": "trivial", "lens": "security-quality", "cycle": "cyc-release-audit", "refs": ["README.md#adding-a-metric"], "depends_on": [], "blocked_by": [], "related": ["fnd-021"], "evidence": ["declarative.py:47-73 _validate", "_validate now rejects any url whose scheme is not https — http, file and ftp all raise", "_validate rejects < or > in key, label, unit, category, description and source", "9 new tests in tests/test_declarative.py, written failing first: 3 url schemes and 6 displayed fields", "suite 48 -> 57 passing"], "updated": "2026-09-07", "notes": "Reject urlparse(spec['url']).scheme != 'https'. Cheap, and it is the same seam that must reject markup in labels. FIXED, and widened past the original remedy. The row asked only for a scheme check; the markup guard moved here too, because refusing markup at ingest is cheaper than trusting eighteen interpolation sites to keep escaping it, and because it closes fnd-021's residual fmt() path. Raised from nice-to-have to important: it is half of the pair that makes accepting an outside spec safe.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-038", "kind": "finding", "title": "Two adapters bypass the retry and backoff discipline the others use", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "security-quality", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-016"], "evidence": ["cdc_places.py:98-121", "city_profile.py:125-141", "the row understated it: Cache.fetch_url had no retry either, so Zillow, the EPA summaries, all six declarative specs and the map topology had none", "one Retry class, injected, used by cache.py, cdc_places, city_profile, landfire and nasa_power", "narrower than the loops it replaces: a 404 now raises at once instead of spending four attempts and ten seconds of backoff re-confirming a moved file", "also_transient lets NASA declare KeyError and ValueError retryable, since that endpoint answers 200 with an error body — opt-in, so one quirk does not make every bug retryable", "17 tests with an injected sleep, covering growth, exhaustion, each retryable status and each permanent one"], "updated": "2026-09-07", "notes": "Route both through the shared discipline rather than leaving two hand-rolled fetch paths. FIXED, and raised from nice-to-have: with fnd-020 in place a source failure costs that source's metrics rather than the run, which turned a transient blip from a visible crash into a quiet loss of five metrics from CDC PLACES or six from the specs. Retrying and not-retrying are both part of it — the old loops retried a 404 four times.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-039", "kind": "finding", "title": "An installed wheel ships no specs/, so six metrics vanish silently", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "reproducibility", "cycle": "cyc-release-audit", "refs": ["CLAUDE.md#architecture"], "depends_on": [], "blocked_by": [], "related": ["fnd-007", "fnd-012"], "evidence": ["workspace.py:61 does not warn when spec_dir is missing or empty", "`refugia metrics` warns when spec_dir is absent, since a command whose purpose is an authoritative key list must not silently print 15 of 21", "covered by a test that runs it against an empty root", "specs moved to src/refugia/specs/, which is what a wheel carries", "Workspace registers the packaged specs and then any in <root>/specs, so a user's own still work and a duplicate key raises rather than shadowing silently", "verified end to end: built a wheel, confirmed all 6 spec files inside it, installed it into a clean venv and ran `refugia metrics` from /tmp — 21 metrics, where it would have been 15", "a test asserts 21 metrics register from an unrelated directory, and another that a user spec beside the project makes it 22"], "updated": "2026-09-07", "notes": "Warn when spec_dir is absent or empty, or move specs/ under src/refugia/ so packaging carries it. Silence here looks like a working install with a shorter metric list. Partly addressed: the warning exists on the one command where a short list is most misleading. fetch and the other paths still say nothing, and moving specs/ under src/refugia so a wheel carries them is the actual fix. FIXED properly rather than by warning. The old test asserting a short list plus a warning was deleted and replaced by one asserting the opposite — that is the shape of this fix. README, CONTRIBUTING and CLAUDE.md all named the old path and were updated. AMENDED by cyc-release-audit-2: the packaging half holds, but a local spec can now only add, never replace, so a rotted shipped URL has no supported repair path short of editing site-packages.", "finding_kind": "cycle-1", "verified_by": "contradicted by cyc-release-audit-2", "batch": ""}
{"id": "fnd-040", "kind": "finding", "title": "The city-only metric set is constructed rather than injected", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "security-quality", "cycle": "cyc-release-audit", "refs": ["CLAUDE.md#local-conventions"], "depends_on": [], "blocked_by": [], "related": ["fnd-074"], "evidence": ["city_profile.py:29-43 CITY_ONLY", "city_profile.py:64 constructs the vegetation source", "CITY_ONLY moved to metrics/city_only.py — metric declarations were living in the artifact layer, above the layer that owns them", "CityProfile takes its vegetation source and its Retry by injection, defaulting to the obvious construction", "the built page still carries city_only_metrics in its payload"], "updated": "2026-09-07", "notes": "Move CITY_ONLY into metrics/ and inject the source, or write the exception into CLAUDE.md. Dependencies are injected is a stated convention and this is the one place it does not hold. FIXED both halves. The injection is what made CDC PLACES and city_profile testable without a network, which is the reason it was worth doing rather than a tidiness argument. CONTRADICTED by cyc-release-audit-2: the parameter was added but builder.py never passes it, so CityProfile still constructs its own LANDFIRE source with a default Sampling and a non-default --radius-km mislabels city figures.", "finding_kind": "cycle-1", "verified_by": "contradicted by cyc-release-audit-2", "batch": ""}
{"id": "fnd-041", "kind": "finding", "title": "The devenv config carries inputs refugia does not need", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "reproducibility", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-004", "fnd-047"], "evidence": ["devenv.config.toml [binary_cache] block", "devenv.yaml claude-code-nix, superpowers, nix-unstable inputs and allow_unfree", "claude-code-nix, nix-unstable, superpowers, superpowers-marketplace, nixpkgs-python, allow_unfree and permitted_unfree_packages all removed from devenv.yaml", "devenv.config.toml deleted, taking the cachix binary_cache block with it"], "updated": "2026-09-07", "notes": "None are needed to build or test refugia, and each is a piece of the owner's personal tooling made public for no gain. Drop or comment them — and note that every key in devenv.config.toml is layer schema, so it goes entirely if the layer is inlined. FIXED alongside fnd-004: dropping the layer removed the config file these lived in, so nothing points at the owner's personal tooling any more.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-042", "kind": "finding", "title": "The results table and the map are not keyboard reachable", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "medium", "lens": "critique", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-023"], "evidence": ["template.html:2010-2016 and :2517 sortable th elements", "keydown handling would sit beside :2062", "sortable headers: tabindex 0, role=button, aria-sort, Enter and Space; aria-sort describes the order on screen, which for rank is the opposite of the comparator's sign", "a roving tabindex on the rows: 300 rows, one tab stop, arrows to move, Home/End/PageUp/PageDown, Enter to open", "draw() replaces every th and tr, so focus had to be restored after sorting and after selecting — without that the route existed and was unusable", "openPanel moves focus into the dialog and closePanel returns it to the selected row: showModal() would do both, but the artifact frame is sandboxed without allow-modals so the dialogs open by attribute and nothing did", "verified by driving the built page: arrow to a row, Enter opens the panel with focus on Close inside it, Escape closes and focus lands back on the same row, still one tab stop"], "updated": "2026-09-07", "notes": "Give sortable th tabindex=0, role=button and aria-sort with a keydown handler, and make table rows focusable so county selection has a keyboard route. Full SVG keyboard navigation is not worth it — the table is the accessible view of the same data. FIXED, and it found two defects beyond the row's remedy. The naive version — tabindex on every row — would have put three hundred tab stops between the table and anything after it, which is worse than no keyboard route because the table becomes something to escape. And a modal opened by attribute rather than showModal() moves no focus at all, so a reader could open a county panel and land on the body with the panel unreachable. Neither was visible without actually pressing the keys.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-043", "kind": "finding", "title": "Declining a Socrata app token is defensible and should be recorded as a choice", "status": "declined", "severity": "P2", "release_gate": "none", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-016"], "evidence": ["a cold fetch makes roughly ten Socrata requests in total"], "updated": "2026-09-07", "notes": "An optional SOCRATA_APP_TOKEN header would raise the rate limit, but the key-free posture is a virtue for a tool strangers are asked to run and ten requests do not need it. Recorded so it is not re-raised as an oversight.", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-044", "kind": "finding", "title": "One Oregon state code remains in a test as a negative control", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "personal-data", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-002", "fnd-050"], "evidence": ["tests/test_geometry.py:58 uses \"41\"", "tests/test_geometry.py now uses \"06\" as the CONUS negative control"], "updated": "2026-09-07", "notes": "Change to \"06\" only if the grep in fnd-002 must come back truly clean. The test needs some CONUS state code and this one is not a tell; see fnd-050. FIXED. The assertion needed some CONUS state code and never needed that one; the grep for the author's county now comes back clean across the whole tree.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-045", "kind": "finding", "title": "No credentials, tokens, emails or host paths in any tracked file or any commit", "status": "declined", "severity": "P2", "release_gate": "none", "effort": "trivial", "lens": "personal-data", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-003", "fnd-011", "fnd-072"], "evidence": ["git grep -nIiE 'api[_-]?key|token|secret|password|Bearer' hits only README.md:134 ('per token') and devenv.config.toml:15 (provider = \"none\")", "no email in any tracked file", "the only /Users/ string is inside the .claude/settings.json symlink blob"], "updated": "2026-09-07", "notes": "Checked and clean across all 21 commits. Use this as the post-rewrite checklist rather than re-running the sweep from scratch. AMENDED by cyc-release-audit-2: the address also survives in the v0.1.0 tagger field, which a sweep over commits alone would miss.", "finding_kind": "cycle-1", "verified_by": "contradicted by cyc-release-audit-2", "batch": ""}
{"id": "fnd-046", "kind": "finding", "title": "Git history carries no removed data artifact", "status": "declined", "severity": "P2", "release_gate": "none", "effort": "trivial", "lens": "reproducibility", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-003"], "evidence": ["all-history added paths (68) equals git ls-files (68) — nothing was ever added and deleted", ".git is 3.0 MB; largest blob in all history is template.html at 105,848 bytes", "data/cache/*, data/dataset.json and out/ correctly gitignored at .gitignore:22-26 and confirmed untracked, including the local 87 MB / 6,861-file cache and the 3.4 MB generated page"], "updated": "2026-09-07", "notes": "There is no orphaned blob, so the history question in fnd-003 is purely about the two live files.", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-047", "kind": "finding", "title": "REFUTED — the cachix binary cache is publicly readable", "status": "declined", "severity": "P2", "release_gate": "none", "effort": "trivial", "lens": "reproducibility", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-004", "fnd-041"], "evidence": ["curl https://krimsonkla.cachix.org/nix-cache-info returns HTTP 200 with a valid body"], "updated": "2026-09-07", "notes": "The cache at devenv.config.toml:18-20 was raised as a private-infrastructure dependency. A forker gets cache hits, not an error. Do not re-raise. Moot as of the layer removal — the cache reference lived in devenv.config.toml, which is deleted.", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-048", "kind": "finding", "title": "REFUTED — .claude/settings.json does not expose hooks, permissions or an allowlist", "status": "declined", "severity": "P2", "release_gate": "none", "effort": "trivial", "lens": "personal-data", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-011"], "evidence": ["only the 106-byte symlink text is in the repository", "the 18 KB body with defaultMode: acceptEdits, the Bash allowlist and ~40 nix-store hook commands lives in a content-addressed store file that exists on no other machine and is never transmitted by a clone"], "updated": "2026-09-07", "notes": "The finding was raised as a permissions leak and verified down to a dangling file and a leaked home path. fnd-011 carries the real, much smaller version.", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-049", "kind": "finding", "title": "REFUTED — a contributor is not locked out of the test suite", "status": "declined", "severity": "P2", "release_gate": "none", "effort": "trivial", "lens": "reproducibility", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-004"], "evidence": ["from a fresh clone with VIRTUAL_ENV and PYTHONPATH unset, uv sync --all-groups && uv run pytest gives 48 passed in 0.27s off the committed uv.lock, touching nothing from the private layer"], "updated": "2026-09-07", "notes": "Only the documented path is broken, not the code. This is why fnd-004 is a small documentation fix rather than a packaging rewrite.", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-050", "kind": "finding", "title": "REFUTED — the Oregon references in test fixtures are not a personal tell", "status": "declined", "severity": "P2", "release_gate": "none", "effort": "trivial", "lens": "personal-data", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-002", "fnd-044"], "evidence": ["test_geometry.py:58 needs some CONUS state code as a negative control", "conftest.py uses Oregon/Idaho/Maine/Ohio with synthetic FIPS 00001-00004 and NATO-alphabet names"], "updated": "2026-09-07", "notes": "The spread reads as deliberate. Once tests/test_profile.py:45-46 is fixed there is no residue. conftest.py and test_engine.py need nothing. AMENDED by cyc-release-audit-2: the declining evidence covered names and FIPS, not the fixture coordinates, which sit in central Oregon.", "finding_kind": "cycle-1", "verified_by": "contradicted by cyc-release-audit-2", "batch": ""}
{"id": "fnd-051", "kind": "finding", "title": "Linting and dependency posture are healthy", "status": "declined", "severity": "P2", "release_gate": "none", "effort": "trivial", "lens": "security-quality", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-008"], "evidence": ["ruff check . — all checks passed", "pylint src/refugia — 10.00/10", "three runtime deps: httpx, typer, openpyxl", "uv.lock committed with hashes; uv lock --check resolves 37 packages cleanly at current versions", "no getenv or environ reads anywhere in src/"], "updated": "2026-09-07", "notes": "No supply-chain or code-hygiene work is owed before publication. CI in fnd-008 is about keeping it that way, not about fixing it.", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-052", "kind": "finding", "title": "The scoring core is the best-tested code in the repo", "status": "declined", "severity": "P2", "release_gate": "none", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-013", "fnd-025"], "evidence": ["engine.py 100%, profile.py 100%, scored_place.py 100%, dataset.py 100%, criterion.py 94%, normalizer.py 93%"], "updated": "2026-09-07", "notes": "Recorded because README.md:152 claims the opposite and the test gaps in fnd-012 are all at the edges, not in the scoring core.", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-053", "kind": "finding", "title": "The test suite is offline by construction, not by luck", "status": "declined", "severity": "P2", "release_gate": "none", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-012", "fnd-014"], "evidence": ["the two URL-using tests pre-seed a Cache(tmp_path) so cache.py:52-53 short-circuits before httpx.get", "the only URLs in tests/ are https://example.invalid/..."], "updated": "2026-09-07", "notes": "A contributor can run the suite on a plane with no keys and no cache. Worth stating in CONTRIBUTING.md as a property rather than leaving it to be rediscovered.", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-054", "kind": "finding", "title": "No API keys, accounts or environment variables are needed by any source", "status": "declined", "severity": "P2", "release_gate": "none", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-006", "fnd-018"], "evidence": ["no getenv, environ or api_key anywhere in src/"], "updated": "2026-09-07", "notes": "A genuine adoption advantage and currently unstated in the README. fnd-006 covers saying so.", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-055", "kind": "finding", "title": "The PyPI name 'refugia' is unclaimed and a trademark conflict is implausible", "status": "declined", "severity": "P2", "release_gate": "none", "effort": "trivial", "lens": "critique", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-032"], "evidence": ["pypi.org/project/refugia returns 404"], "updated": "2026-09-07", "notes": "'Refugia' is a standard ecological term. Nothing to do unless the name is worth reserving.", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-056", "kind": "finding", "title": "The published artifact, not the repo, is the redistribution surface — and it carries CHR&R data", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "data-licensing", "cycle": "cyc-release-audit", "refs": ["README.md#use"], "depends_on": [], "blocked_by": [], "related": ["fnd-009", "fnd-010", "fnd-024", "fnd-034", "fnd-092"], "evidence": ["the repo vendors no upstream data: git grep finds only URLs, and data/cache/.gitkeep is the sole tracked file under data/", "builder.py:31-41 embeds dataset.to_dict() plus county paths, state paths, centroids, city markers and city metrics into out/refugia.html", "five of the six specs draw on County Health Rankings, whose content is all-rights-reserved under a personal/non-profit-only licence", "template.html:2678-2679 builds the credit line from registered metrics only", "the page carries the CHR&R citation, the Zillow attribution and the us-atlas ISC notice, built from the metrics themselves (fnd-009, fnd-060, fnd-061, fnd-019)", "README's 'Data and licence' section says running the tool is not redistribution and sharing the page is", "`publish` now prints what it just made: how many places are embedded, that the profile travels with it, and how many sources require attribution — counted from the metrics so it cannot assert an obligation that no longer applies", "6 tests written failing first; suite 111 -> 117"], "updated": "2026-09-07", "notes": "Publishing the code is unaffected by any upstream terms — the repo ships URLs, not data, and each user fetches under their own licence. The obligation attaches to the generated page, which embeds CHR&R-derived values for ~1,900 counties and geometry derived from ISC-licensed us-atlas. Consequences: the page must carry the CHR&R citation, the Zillow attribution and the us-atlas ISC notice (fnd-009); a GitHub Pages demo (fnd-034) is the owner redistributing CHR&R content and needs those credits in place first; and the README should tell users a page they share carries the same obligations. Commercial use of a shared page needs CHR&R's prior written consent, so a non-commercial framing (fnd-026) is worth stating deliberately rather than by omission. FIXED. Three of the four consequences were closed by the rows this one pointed at; the gap nobody covered was that `publish` said nothing at all. The person most likely to send the file is the one who just built it, and they are the least likely to have read DATA_SOURCES.md — that command is the only moment which reaches them. What this row unblocked rather than did: fnd-034, the Pages demo, which needed the credits in place first and now has them; and fnd-026's non-commercial framing, which the README states but which is still worth a deliberate project-status line. AMENDED by cyc-release-audit-2: the screenshots do embed readable CHR&R values, so 'the repo vendors no upstream data' is not exact.", "finding_kind": "cycle-1", "verified_by": "contradicted by cyc-release-audit-2", "batch": ""}
{"id": "fnd-057", "kind": "finding", "title": "County Health Rankings is non-commercial-only, which constrains what refugia may become", "status": "parked", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "data-licensing", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-009", "fnd-056", "fnd-062", "fnd-026"], "evidence": ["countyhealthrankings.org/terms-use — licence is 'for personal or non-profit purposes including educational, research, or public health'", "same page — content 'may not be copied or otherwise used for commercial use ... without CHR&R's express prior written consent'", "all content 'is the property of CHR&R or its licensors with all rights reserved' — not public domain", "five of six specs: air_pollution, life_expectancy, poor_mental_health_days, poor_or_fair_health, social_associations"], "updated": "2026-09-07", "notes": "The binding constraint on the whole project, and the only source that is not freely licensed. Publishing the code is unaffected — the repo ships URLs, not data. What it forecloses is a commercial form: a hosted service, a paid tier, or anything sold on top of a page carrying these five metrics needs CHR&R's prior written consent. Discussion needed on whether to state a non-commercial position explicitly, drop to the federal sources underneath the CHR&R compilation (their analytic file is the licensed thing, not the underlying facts), or ask CHR&R for written consent. Doing nothing is defensible for a personal project and is the current de facto position.", "review_at": "before any hosted, paid or commercial form of refugia", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-058", "kind": "finding", "title": "Wildfire Risk to Communities publishes no licence and has non-federal co-authors", "status": "parked", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "data-licensing", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-010", "fnd-009"], "evidence": ["wildfirerisk.org/about — USDA Forest Service with Headwaters Economics and Pyrologix (a Vibrant Planet company)", "no terms of use, licence or citation statement found on the site", "specs/wildfire_risk.json pulls wrc_download_20260415.xlsx"], "updated": "2026-09-07", "notes": "Public domain does not follow automatically from USDA funding when non-federal partners co-produced the work. Nothing suggests the data is restricted and it is published for public download, so the practical risk is low — but the position is inferred, not established. Options to discuss: label it as unverified in DATA_SOURCES.md and ship, email the project to ask, or drop the metric. Recommendation is to label and ship.", "review_at": "if the project ever states terms, or if a takedown is requested", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-059", "kind": "finding", "title": "LANDFIRE public-domain status was never independently verified", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-010", "fnd-009"], "evidence": ["landfire_vegetation.py fetches lfps.usgs.gov and landfire.gov LF2024_EVT.csv", "assumed USGS/USFS public domain; not checked against a published statement", "landfire.gov/data and landfire.gov/data/citation read on 2026-09-07", "they publish a citation model and a limitations disclaimer, and no statement about copyright, public domain or redistribution", "DATA_SOURCES.md now records this as checked-and-unpublished rather than unchecked"], "updated": "2026-09-07", "notes": "The most likely-benign item on the list — LANDFIRE is a USGS/USFS programme and its products are routinely treated as public domain. Ten minutes on their data-access page settles it. Do not assert the licence in DATA_SOURCES.md until someone has. FIXED, though not with the answer expected. The check was the deliverable and the result is that LANDFIRE says nothing about licensing at all. Federal public domain remains an inference — but now an inference somebody actually went and tested, which is the difference the row was asking for.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-060", "kind": "finding", "title": "us-atlas is ISC-licensed and its notice must travel with the generated page", "status": "fixed", "severity": "P2", "release_gate": "important", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-009", "fnd-056"], "evidence": ["geometry.py:9 fetches cdn.jsdelivr.net/npm/us-atlas@3/counties-10m.json", "us-atlas is ISC, a redistribution of Census cartographic boundary shapefiles", "builder.py:31-34 embeds projected county paths, state paths and centroids into the page", "template.html:2678-2679 credits registered metrics only, so us-atlas appears nowhere", "CREDITS block in the page footer names us-atlas with a link, Copyright Michael Bostock, ISC licence, and that it redistributes the Census cartographic boundary files", "the Census geography and city files are credited in the same block — the sources line is built from registered metrics and structurally cannot show them", "verified rendered in the built page"], "updated": "2026-09-07", "notes": "ISC is permissive but conditions redistribution on retaining the copyright notice, and the artifact embeds paths derived from the topology. This is the only outright licence-condition breach found, and it is one line in the footer. The Census geometry underneath is public domain; the ISC notice covers Mike Bostock's compilation. FIXED: the only outright licence-condition breach found in the audit, closed by one footer block.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-061", "kind": "finding", "title": "Zillow requires clear attribution and gets a bare four-character token", "status": "fixed", "severity": "P2", "release_gate": "important", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-009", "fnd-056"], "evidence": ["zillowgroup.com/developers/api/public-data/real-estate-metrics — 'free for public use by consumers, media, analysts, academics and policymakers' and 'Proper and clear attribution of all data to Zillow is required'", "zillow_home_value.py:41 emits the source string \"Zillow ZHVI\"", "the stricter no-derivative-works licence applies to the Public Records Data API, not the research CSVs", "zillow_home_value.py:41 source is now \"Zillow Home Value Index (ZHVI)\"", "the footer credits block names the index in full and links to zillow.com/research/data", "note: a page built from an existing data/dataset.json still shows the old token, since the source string is stored with the metric at fetch time"], "updated": "2026-09-07", "notes": "The permission is clear and the use fits squarely inside it; only the attribution is thin. Name Zillow and the index in full and link to zillow.com/research/data from the artifact footer and DATA_SOURCES.md. Not a blocker, but it is the cheapest way to be unambiguously inside the terms of the one commercial source in the set. FIXED. The stale-dataset caveat is worth knowing: the source string travels in dataset.json, so the shortened token persists until the next fetch. That is the same structural point fnd-019 makes about attribution not travelling with a metric.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-062", "kind": "finding", "title": "CHR&R's terms name AI-model training, and refugia ships an LLM feature", "status": "parked", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-057", "fnd-009"], "evidence": ["CHR&R terms bar use 'to train AI models intended to be offered as commercial products' without consent", "ask/planner.py sends metric values to a local Ollama instance at localhost:11434", "README.md#asking-questions documents the feature"], "updated": "2026-09-07", "notes": "Almost certainly fine as built: local inference is neither training nor commercial, and the clause targets model training. Logged because it is the intersection of a named restriction and a shipped feature, and because a future change — a hosted ask endpoint, a fine-tune on the dataset — moves it from fine to not. Worth a sentence in DATA_SOURCES.md recording that the read was made rather than leaving it to be rediscovered.", "review_at": "if ask is ever hosted, or the dataset used to fine-tune a model", "finding_kind": "cycle-1", "verified_by": "", "batch": ""}
{"id": "fnd-063", "kind": "finding", "title": "The README still offers only the nix path, though the uv path is what most people have", "status": "fixed", "severity": "P2", "release_gate": "important", "effort": "small", "lens": "reproducibility", "cycle": "cyc-release-audit", "refs": ["README.md#use", "CLAUDE.md#two-ways-in"], "depends_on": [], "blocked_by": [], "related": ["fnd-004", "fnd-006", "fnd-014", "fnd-049"], "evidence": ["README.md:49-52 prefixes every command with 'devenv shell -- '", "no Install section exists above README.md:46", "verified from a fresh clone with VIRTUAL_ENV and PYTHONPATH unset: uv sync --all-groups && uv run pytest gives 48 passed, and uv run refugia --help works", "CLAUDE.md now documents both paths; the README does not", "README has an Install section: Python >=3.12, uv, git clone, uv sync --all-groups", "every command in Use is now `uv run refugia ...`; devenv is described as a maintainer convenience", "the publish line's `writes out/refugia.html` comment checked against cli.py's actual default"], "updated": "2026-09-07", "notes": "The residue of fnd-004 after the layer removal. This is no longer about access — devenv.sh is public and installable, so the nix path works for anyone now — it is about not asking a reader to install nix to try a Python tool. Add an Install section: Python >=3.12, uv sync --all-groups, uv run refugia fetch, and relabel the devenv lines as the maintainer path. Phase 3 work; the escape route is already verified. FIXED. Also states that no API keys or accounts are needed, which was fnd-006(d) and belongs next to the install rather than three sections later. A claim that CI runs the uv path was drafted and removed: there is no CI yet (fnd-008).", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-064", "kind": "finding", "title": "The sources line interpolates m.source into innerHTML without escaping", "status": "fixed", "severity": "P2", "release_gate": "important", "effort": "trivial", "lens": "security-quality", "cycle": "cyc-release-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-021", "fnd-037"], "evidence": ["template.html footer: [...new Set(M.map((m) => m.source))].join(' · ') concatenated into innerHTML", "missed by fnd-021's audit, whose scan looked for ${...} template-literal interpolations and this is a .join()", "now wrapped: M.map((m) => esc(m.source))"], "updated": "2026-09-07", "notes": "Found while editing the same line for fnd-060. Worth its own row rather than folding into fnd-021, because the lesson is about the audit and not the bug: a scan built around one syntax for interpolation will not see the other. Any future sweep should look for concatenation into innerHTML as well as template literals.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-065", "kind": "finding", "title": "The network adapters are thinly tested, and that is where the surprises live", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "large", "lens": "tests", "cycle": "cyc-release-audit", "refs": ["README.md#known-gaps"], "depends_on": [], "blocked_by": [], "related": ["fnd-012", "fnd-015", "fnd-016", "fnd-020"], "evidence": ["landfire_vegetation 35%, nasa_power_climate 32%, city_profile 21%, city_markers 23%, geometry 27%, places/registry 33%, ask/planner 32%", "total coverage 60%; the scoring core, the store and the declarative tier are all above 92%", "README already states that network adapters are thinly tested", "cdc_places 44% -> 100%: paging across the 50,000 cap, missing values skipped rather than zeroed, the empty-result guard, cache reuse, retry on a dropped connection and no retry on a 404", "store/cache 100%, retry 94%; total 54.58% -> 58.02% and the CI floor raised from 54 to 57", "zillow_home_value 41% -> 97% and epa_air_quality 50% -> 93%, both tested by seeding the cache rather than by stubbing a client", "the cases are the ways a parser actually fails: Zillow's most recent month column is routinely blank, and the EPA pads county names on a join that is by name", "mutation-checked: taking the first populated month instead of the last, and dropping the strip() from the EPA join, each fail a test"], "updated": "2026-09-07", "notes": "Accepted rather than urgent, and stated so the 60% is not mistaken for neglect of something nobody noticed. These are the modules whose behaviour is defined by an upstream's quirks — a 200-shaped error body, a raster that returns NoData across half a continent, a workbook whose header row moved — and testing them properly means recording real payloads as fixtures, which is a day's work and a maintenance burden of its own. The parts that can be tested without the network already are: the cache, the throttle, the empty-result guards, the fetch-loop resilience and the spec validator all have tests, and each of those was where a real defect was actually found. Materially advanced, not closed. CDC PLACES was the right one to take first — five metrics, and the adapter a single dropped connection used to cost outright — and it went to 100% only because fnd-038 and fnd-040 made it injectable. What remains is landfire (30%), nasa_power (26%), city_profile (16%), geometry, city_markers, places/registry and the planner. Each needs recorded payloads as fixtures, which is the day's work the row already describes. Advanced again. Zillow and the EPA were the right pair to take next: both are pure parsing over a cached payload, so seeding the cache tests the real code path without inventing a client. What is left is the three that genuinely need recorded payloads — landfire, nasa_power and city_profile — plus the geometry and place registries.", "finding_kind": "cycle-1", "verified_by": "", "batch": "tests-that-bite"}
{"id": "fnd-066", "kind": "finding", "title": "The README carried six jobs at once and was 452 lines", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "medium", "lens": "user-docs", "cycle": "cyc-release-audit", "refs": ["README.md"], "depends_on": [], "blocked_by": [], "related": ["fnd-006", "fnd-007", "fnd-022", "fnd-034"], "evidence": ["docs/guide/ with six pages plus an index; README 452 -> 304 lines", "four pages are the existing prose moved rather than rewritten; adding-a-spec and adding-an-adapter are new", "tests/test_guide.py asserts every repository path the docs name exists, every relative link resolves, every snake_case token is a real metric key, spec field or universe, and that the index links every page", "universes come from places.registry.UNIVERSES rather than a hardcoded list, so renaming one fails the test", "mutation-checked: pointing a page at a moved spec file fails it"], "updated": "2026-09-07", "notes": "Raised and fixed together after the owner asked for per-audience docs and declined the Pages site (fnd-034). Kept in the repository rather than a wiki, deliberately: a wiki is a separate git repository, so nothing here could test it, no reviewer would see a doc change beside the code change, and it could not include a file it documents. Those are exactly the properties that let two stale claims survive earlier in this repository. The guard test is what makes the split worth doing rather than a rearrangement — prose is the one part of a project nothing checks, and now something does.", "finding_kind": "cycle-1", "verified_by": "self (the change's author)", "batch": ""}
{"id": "fnd-067", "kind": "finding", "title": "GitHub's repo activity API republishes the pre-rewrite tip b38ccb8 — the leak needs no SHA from anywhere", "status": "open", "severity": "P0", "release_gate": "blocker", "effort": "small", "lens": "personal-data", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["The finder framed the remote exposure as \"latent, requires an attacker to know a SHA\", and framed findings.jsonl as the thing that supplies it. Neither is true. `gh api repos/krimsonkla/refugia/activity?per_page=100` returns, as its last two rows:\n  force_push b38ccb8ae94f10abf7d94e02171b7d6461c3f3d7 -> 98547320dabee9aad1318d0ef376663e93c103fd  ref=refs/heads/main\n  branch_creation 0000000000000000000000000000000000000000 -> b38ccb8ae94f10abf7d94e02171b7d6461c3f3d7\nThat endpoint requires only read access, which is universal on a public repo. From b38ccb8, `gh api 'repos/krimsonkla/refugia/commits?sha=b38ccb8ae9&per_page=30'` returns all 21 pre-rewrite commits — the whole deleted history, not the single initial commit the finder tested. It also happens to be printed in this session's own git-status context line (\"b38ccb8 fix(artifact): reset returns to the setup marked as default\"), which is how easy it is to come by.\n\nThis changes both the size of the exposure and the acceptance test for the fix, which is why it is listed separately rather than folded in.", "paths: docs/data/findings.jsonl (fnd-003)"], "updated": "2026-09-07", "notes": "Treat b38ccb8ae94f10abf7d94e02171b7d6461c3f3d7 as the SHA that matters, not cfb6988. Delete-and-recreate the repository (which discards the activity feed along with the objects) rather than relying on any purge that leaves the feed intact; verify with all three checks listed in the remedy for remote-retains-prerewrite-history.", "batch": "publish-the-repo"}
{"id": "fnd-068", "kind": "finding", "title": "The history rewrite was local only — GitHub still serves the entire pre-rewrite history, including profiles/jason.json with home_fips 41017", "status": "open", "severity": "P0", "release_gate": "blocker", "effort": "small", "lens": "personal-data", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced in full, and the real exposure is larger than the finder measured.\n\nLocal side agrees: `git rev-list --max-parents=0 --all` -> one root, 98547320dabee9aad1318d0ef376663e93c103fd; `git rev-list --all --count` -> 25; the identity sweep (`git grep -nIiE 'deschutes|41017|jasonrisch|jason\\.json'`, `git grep -nIE '/(Users|home)/[a-z]'`) returns nothing across the tracked tree.\n\nRemote side does not. `gh api repos/krimsonkla/refugia/commits/cfb6988df6482625cd52737475df9c93defccc6d` -> HTTP 200, 'feat(refugia): rank US counties against a personal allergen and wellbeing profile'. Its tree serves profiles/jason.json (name: 'Jason — allergens first', personal weights) and .claude/settings.json as a symlink whose target is literally `/nix/store/8pnkcjn40wcl3c7sc30zh5s8b48z4a82--Users-krimsonkla-git-krimsonkla-refugia-.claude-settings.json`.\n\nTHE FINDER UNDERSTATED IT TWICE.\n(a) No SHA needs to be guessed or read out of findings.jsonl. `gh api repos/krimsonkla/refugia/activity?per_page=100` — readable by anyone with read access, i.e. by everyone once the repo is public — ends with:\n  force_push b38ccb8ae9 -> 98547320da  ref=refs/heads/main\n  branch_creation 0000000000 -> b38ccb8ae9\nThat hands out the pre-rewrite tip directly.\n(b) From that tip the whole old history is walkable: `gh api 'repos/krimsonkla/refugia/commits?sha=b38ccb8ae9&per_page=30'` returns 21 commits, and `gh api 'repos/krimsonkla/refugia/contents/profiles/jason.json?ref=b38ccb8ae9'` returns the mature profile — which, unlike the cfb6988 version the finder quoted, does carry \"home_fips\": \"41017\" (Deschutes County, Oregon). The finder attributed home_fips to cfb6988; at cfb6988 the field does not exist yet. Right conclusion, wrong commit.\n\nOne mitigation the finder did not credit: `gh api repos/krimsonkla/refugia/git/trees/b38ccb8ae9?recursive=1` shows no docs/ path at the old tip, so the unscrubbed audit ledger (which fnd-003 says quoted the county, the FIPS and the commit email verbatim) was never pushed. The exposure is jason.json, the .claude symlink target, and the old README/test copies — not the ledger.\n\nAlso confirmed: `git cat-file -t b7ec275` -> 'Not a valid object name' and the API returns 422, so fnd-003's recorded post-rewrite root SHA describes nothing that exists. Repo is still PRIVATE (forkCount 0, isFork false), so nothing is exposed today.", "paths: docs/data/findings.jsonl (fnd-003, status \"fixed\", release_gate \"blocker\"), .git/refs/remotes/origin/main"], "updated": "2026-09-07", "notes": "Force-push does not delete objects on GitHub, and the activity feed republishes the orphaned tip. `git ls-remote` shows only refs/heads/main and refs/tags/v0.1.0 — no refs/pull, no forks — so the clean fix is: delete the GitHub repository, re-create it, push the 25-commit rewritten history and re-create the v0.1.0 tag. That drops the activity feed with the repo and guarantees no pre-rewrite object survives. If the repo must stay in place, open a GitHub Support ticket for a gc/purge of unreachable objects. ACCEPTANCE TEST — all three must hold before flipping visibility, not just the first: (1) `gh api repos/krimsonkla/refugia/commits/b38ccb8ae94f10abf7d94e02171b7d6461c3f3d7` -> 422; (2) `gh api repos/krimsonkla/refugia/commits/cfb6988df6482625cd52737475df9c93defccc6d` -> 422; (3) `gh api repos/krimsonkla/refugia/activity?per_page=100` lists no before/after SHA outside the 25 commits in `git rev-list --all`. Then correct fnd-003's evidence, which currently names a root commit (b7ec275) that does not exist.", "batch": "publish-the-repo"}
{"id": "fnd-069", "kind": "finding", "title": "CONTRIBUTING's \"exactly what CI runs\" list omits the coverage floor, and its addopts paragraph describes a configuration that was deleted", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "contributor-docs", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced every element. CONTRIBUTING.md:41 says \"Exactly what CI runs\" and CONTRIBUTING.md:44-47 lists `uv run pytest`; .github/workflows/ci.yml's Test step is `uv run pytest --cov=refugia --cov-report=term-missing --cov-fail-under=60`. CONTRIBUTING.md:50-53 claims \"`pytest` is configured with coverage on by default ... prints 20% against the whole suite's 59%\"; pyproject.toml:48-53 has no addopts key and a comment saying \"Coverage is not in addopts\". Measured myself: `devenv shell -- uv run pytest tests/test_engine.py` -> \"9 passed in 0.01s\", no coverage line at all; the full run reports \"Total coverage: 60.92%\" (61%, not 59%). Line-number correction: the PR-template checkbox is .github/pull_request_template.md:7, not :8. CLAUDE.md:34 does still read `devenv shell -- pytest  # suite with coverage`. Root cause is real and checkable: tests/test_guide.py:25 sets DOCS = docs/guide/*.md plus README.md only, so CONTRIBUTING.md, CLAUDE.md and .github/ are outside every prose check that exists.", "paths: CONTRIBUTING.md:41-53, .github/workflows/ci.yml (Test step), pyproject.toml:48-53, .github/pull_request_template.md:7, CLAUDE.md:34, tests/test_guide.py:25", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Add `--cov=refugia --cov-report=term-missing --cov-fail-under=60` to the pytest line at CONTRIBUTING.md:47 and to the one-line command at pull_request_template.md:7. Replace CONTRIBUTING.md:50-53 with the current truth: coverage is opt-in, a single-file run prints none, CI measures once over the whole suite against a 60% floor. Change CLAUDE.md:34's comment to `# the suite` (coverage is not on). To stop the recurrence, widen tests/test_guide.py's DOCS to include CONTRIBUTING.md, CLAUDE.md, SECURITY.md and .github/pull_request_template.md, and add one assertion that the pytest invocation in CONTRIBUTING appears verbatim in ci.yml. FIXED: The CONTRIBUTING check list now includes the coverage floor CI enforces, and says the hooks are a different set -- more formatting, no pytest.", "batch": ""}
{"id": "fnd-070", "kind": "finding", "title": "The Throttle rule is stated in four documents and not followed by NasaPowerClimateSource, and no test checks it", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "contributor-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced: `grep -rn Throttle src/` outside throttle.py gives only landfire_vegetation.py:17 and :77. nasa_power_climate.py:49 (`workers: int = 6`) and :125 (`ThreadPoolExecutor(max_workers=self._workers)`) fan out with no Throttle. The rule is asserted at CLAUDE.md:90, CONTRIBUTING.md:115-116, docs/guide/adding-an-adapter.md:72 and pull_request_template.md:26. tests/test_outbound_requests.py checks USER_AGENT on httpx calls only and never looks for a pool, so nothing enforces it. Two corrections to the finder. (a) Volume framing is overstated: NASA's jobs are 4 parameters x 5-degree tiles (nasa_power_climate.py:17-22,123,165-175), on the order of a hundred-odd requests, not the thousands landfire issues — the defect is the unenforced convention, not an imminent rate problem. (b) The finder's secondary claim that landfire \"constructs its Retry and Throttle rather than taking them\" is only half true: landfire_vegetation.py:72-79 takes an injected `Sampling` and derives both from it, so it is configurable, just not substitutable in a test. I also checked artifact/city_profile.py:72, which fans out 8-wide but only through LandfireVegetationSource.cover_at, so it inherits that source's shared Throttle and is not a third violator.", "paths: src/refugia/metrics/sources/nasa_power_climate.py:49,123-133, tests/test_outbound_requests.py, CLAUDE.md:90, CONTRIBUTING.md:115-116, docs/guide/adding-an-adapter.md:72, .github/pull_request_template.md:26", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Give NasaPowerClimateSource a `Throttle` shared across the pool, taken in __init__ the way adding-an-adapter.md:55-58 shows (`throttle: Throttle | None = None`), and call `.wait()` in `_tile` before the request. Then extend tests/test_outbound_requests.py with an AST check that any module referencing `ThreadPoolExecutor` also references `Throttle` — the rule is currently only remembered, and that test exists precisely for the adapter nobody has written yet. FIXED: Same fix as fnd-097.", "batch": ""}
{"id": "fnd-071", "kind": "finding", "title": "The prose-integrity test covers only docs/guide and README, so every document the audit found drifting is outside it", "status": "open", "severity": "P1", "release_gate": "important", "effort": "trivial", "lens": "contributor-docs", "cycle": "cyc-release-audit-2", "finding_kind": "gap-in-enforcement", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["tests/test_guide.py:25 defines DOCS = [*PAGES, ROOT / \"README.md\"] where PAGES is docs/guide/*.md. Its docstring says \"every repository path the guide names must exist, every metric key it names must be registered, and every link between its pages must resolve\" — and it works: I ran my own relative-link and path check over every tracked .md and found zero broken links. But CONTRIBUTING.md, CLAUDE.md, SECURITY.md, CHANGELOG.md and .github/*.md are not in DOCS, and those are exactly the four files this audit found stale (CI command list, addopts paragraph, pre-commit symlink, `specs/` path in CHANGELOG). The finder treated this as a remedy footnote; it is the single change that prevents the recurrence of four of its eight findings.", "paths: tests/test_guide.py:22-28, CONTRIBUTING.md, CLAUDE.md, SECURITY.md, CHANGELOG.md, .github/pull_request_template.md"], "updated": "2026-09-07", "notes": "Add CONTRIBUTING.md, CLAUDE.md, SECURITY.md, CHANGELOG.md and .github/pull_request_template.md to DOCS in tests/test_guide.py. The existing PATHLIKE regex already covers `src/...`, `tests/...`, `.github/...` and root capitals, so this is a one-line change plus whatever it turns red.", "batch": "prose-guard"}
{"id": "fnd-072", "kind": "finding", "title": "The v0.1.0 tag is five commits behind and its code installs 15 metrics instead of the 21 its own CHANGELOG entry claims", "status": "open", "severity": "P1", "release_gate": "important", "effort": "trivial", "lens": "contributor-docs", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["`git log -1 v0.1.0` -> 65188dd; `git rev-list v0.1.0..HEAD --count` -> 5 (63ac87c, d18bb7f, b1abf6e, 42bebe4, 1d3da7d), including a `feat` with no CHANGELOG entry. `git ls-tree -r --name-only v0.1.0 | grep spec` shows the six specs at repo-root `specs/`, and `git show v0.1.0:src/refugia/workspace.py` returns `self._root / \"specs\"` — with [tool.hatch.build.targets.wheel] packages = [\"src/refugia\"], a wheel from the tag carries no specs, so an installed copy registers the five adapters' 15 metrics, exactly what HEAD's workspace.py:40-48 docstring now describes as the bug. CHANGELOG.md:9-24 claims \"21 metrics in seven categories\" (true at HEAD: `refugia metrics` prints 21) and \"JSON specs in `specs/`\" (now `src/refugia/specs/`). I re-ran the build: `uv build` at HEAD produces refugia-0.1.0-py3-none-any.whl containing all six refugia/specs/*.json — same version string as the broken artifact. Severity: important rather than blocker only because the repo is still private and the tag is not yet published; the moment it goes public with this tag in place it is a broken install under a version number that cannot be told apart from the fixed one.", "paths: CHANGELOG.md:9-24, pyproject.toml:3, src/refugia/workspace.py:40-53, git tag v0.1.0 (65188dd)"], "updated": "2026-09-07", "notes": "Before publishing: delete and re-cut v0.1.0 at HEAD, or bump pyproject.toml:3 to 0.2.0 and tag that, with a CHANGELOG entry covering the specs-in-package fix (63ac87c), the shared Retry, and the keyboard route (b1abf6e). Correct CHANGELOG's \"JSON specs in `specs/`\" to `src/refugia/specs/`. Verify the result by installing the built wheel into a clean venv and checking `refugia metrics` prints 21.", "batch": "publish-the-repo"}
{"id": "fnd-073", "kind": "finding", "title": "adding-a-spec.md asserts there is no per-metric code in template.html; the page hard-codes a home_value filter outside the conformance kernel", "status": "open", "severity": "P1", "release_gate": "important", "effort": "medium", "lens": "contributor-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["docs/guide/adding-a-spec.md:25-27 reads \"There is no per-metric code anywhere in `src/refugia/artifact/template.html`\". grep gives nine `home_value` hits in that file: :1154 (`filters = { population: 0, home_value: 0, ... }`), :1337-1340 inside `passes()`, :1535 a hard-coded `<label for=\"f-hv\">Maximum home value (USD...)</label>`, :1580, :2783, :3058, :3217. `devenv shell -- uv run refugia metrics` lists `home_value` as a registered metric key (cost, lower is better, USD) supplied by ZillowHomeValueSource. The finder's scoping is right: `population` and `state` in the same function are Place attributes, not metric keys, so `home_value` is the only genuine violation. The Python side has no counterpart (grep for home_value across src/**/*.py hits only workspace.py, sources/__init__.py and zillow_home_value.py). The secondary point also checks out: the kernel markers are template.html:1215 and :1310, and `passes()` starts at :1333, i.e. outside them, so tests/conformance/vectors.json structurally cannot fail a filtering divergence — which makes CONTRIBUTING.md:103's \"`tests/conformance/vectors.json` will fail you if it is not\" overbroad. Severity stays important rather than blocker: nothing is broken at runtime, but this is the invariant repeated in CLAUDE.md, CONTRIBUTING.md and pull_request_template.md:31.", "paths: docs/guide/adding-a-spec.md:25-27, src/refugia/artifact/template.html:1154,1333-1341,1535,1580,2783,3058,3217, CONTRIBUTING.md:100-105, CLAUDE.md (Local Conventions), .github/pull_request_template.md:31"], "updated": "2026-09-07", "notes": "Cheapest honest fix: reword adding-a-spec.md:25-27 to \"one exception, the maximum-home-value box, which predates the generic cuts mechanism\" and record it. Proper fix: express the home-value box through the existing `cutsAllow` per-metric cuts path (template.html:1319-1331) and delete the four special-cased sites, which also brings it under the vectors. Separately narrow CONTRIBUTING.md:103 to say the vectors cover the kernel between the BEGIN/END markers only, not `passes()` or the requirement filters.", "batch": "scoring-parity"}
{"id": "fnd-074", "kind": "finding", "title": "CityProfile constructs its own LANDFIRE source, so 72% of the fan-out is configured where the composition root cannot see it", "status": "open", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["builder.py:57 is `CityProfile(self._cache)` with no vegetation argument; city_profile.py:56 then falls back to `LandfireVegetationSource(cache)` with a default Sampling and its own Throttle, while workspace.py:78 injects `Sampling(radius_km=self._radius_km)` for the county source only. The radius is interpolated into the metric descriptions at landfire_vegetation.py:82-83 and rendered as the hint text at template.html:1452, so after `refugia fetch --radius-km 50` the page reads 'Share of land within 50 km …' over city figures sampled at 25 km. The 4797/6622 share reproduces from the cache. Two mitigations the finder does not mention: both workspace.py:26 and cli.py:22 default to 25.0, so the mislabel only appears when a user changes the radius; and the city pool is throttled by its own Throttle at the same interval, so today's rate policy does happen to reach it — it is any *future* change to Sampling that would not. It is a straightforward breach of CLAUDE.md's injection rule.", "paths: src/refugia/artifact/builder.py:44-58, src/refugia/artifact/city_profile.py:48-56, src/refugia/workspace.py:78"], "updated": "2026-09-07", "notes": "Have Workspace construct one LandfireVegetationSource and pass the same instance to both build_registry() and ArtifactBuilder (which needs the constructor argument to carry it to CityProfile). CityProfile's injection point already exists; only builder.py and workspace.py change. Add a test that a non-default radius reaches the city sampler.", "batch": "upstream-resilience"}
{"id": "fnd-075", "kind": "finding", "title": "The LANDFIRE throttle binds — the city fan-out runs pinned to the 0.05s floor at ~15 req/s, not under an unreached ceiling", "status": "open", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced independently over data/cache (6,861 entries): landfire-city n=4797, span 319.0s, 15.04 req/s, median inter-request gap 0.0524s against sampling.py:22 min_interval=0.05 — the pool sits on the floor, so 20/s is the operative rate. 8 workers at 15/s is 0.53s of wall time per request, so fnd-016's note premise 'a second or two per request' is wrong by ~3x and its conclusion 'the real rate is already well under 20/s' is false for the city pool. One correction to the finder: its county figure (n=1825, 3.1 min, 9.85/s) does not reproduce over the whole cache — I measure 1828 entries spanning 12,572s at 0.15/s overall; it measured a contiguous burst. Median gap 0.070s does reproduce. The code fix itself (Throttle, lock-held, shared across the pool, throttle.py:36-45; landfire_vegetation.py:77,233) is real and correct — what is wrong is the recorded reasoning for the number chosen.", "paths: src/refugia/metrics/sources/sampling.py:22, src/refugia/metrics/sources/landfire_vegetation.py:77, docs/data/findings.jsonl (fnd-016 notes), DATA_SOURCES.md"], "updated": "2026-09-07", "notes": "Replace fnd-016's closing sentence with the measured figure ('the pool runs at the floor: 15 req/s sustained for five minutes on a cold fetch'), then re-choose Sampling.min_interval knowing it is the operative rate, not a bound — 0.2s gives 5/s and costs a cold fetch roughly twenty minutes instead of eight. State the chosen outbound rate in DATA_SOURCES.md's LANDFIRE entry so a forker sees what they are about to send to lfps.usgs.gov.", "batch": "upstream-resilience"}
{"id": "fnd-076", "kind": "finding", "title": "The structural User-Agent guard recognises only httpx.get/httpx.post on a name literally called `httpx`", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["tests/test_outbound_requests.py:25-27 is the whole matcher. I ran it over a synthetic module containing httpx.Client().get, a stored client's .get/.post, `from httpx import get as hget; hget(...)`, httpx.request, httpx.stream and requests.get — it printed `matched: []`. Nothing else in tests/ forbids `import requests` or `urllib.request`, and test_the_check_is_actually_finding_call_sites only asserts >= 5, which the existing five satisfy regardless of what a sixth adapter does. fnd-016's evidence line ('walks the AST of every module and fails any httpx call without the shared USER_AGENT') therefore overstates what the guard covers, and that guard is the row's stated main value.", "paths: tests/test_outbound_requests.py:20-33, tests/test_outbound_requests.py:47-49", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Match any call whose func resolves into httpx — attribute calls on `httpx`, httpx.Client/AsyncClient construction and the `.get/.post/.request/.stream/.send` calls on a client, and from-imports of httpx names — and add a separate test that fails on `import requests` or `urllib.request` anywhere under src/. Mutation-check it by adding a Client-style call site with no headers and confirming the suite goes red, not only by deleting a header from an existing call. FIXED: The AST sweep now matches any httpx-rooted call across ten verbs plus Client/AsyncClient construction, scoped by walking the receiver chain so a dict .get() is not flagged. A probe using httpx.Client().get() and httpx.request() is caught at four sites. A second test forbids importing another fetching client.", "batch": ""}
{"id": "fnd-077", "kind": "finding", "title": "docs/data/findings.jsonl prints a pre-rewrite SHA that still resolves, and ships in the sdist", "status": "open", "severity": "P1", "release_gate": "important", "effort": "trivial", "lens": "personal-data", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["The facts are right, the severity is not.\n\nConfirmed: findings.jsonl line 3 (fnd-003) contains \"the personal profile and .claude/settings.json both introduced in cfb6988, the initial commit\", line 11 (fnd-011) contains \"blob 67efc6a\", and cfb6988 resolves on the remote. Confirmed the sdist claim by building it: `uv build --sdist` -> refugia-0.1.0.tar.gz contains refugia-0.1.0/docs/data/findings.jsonl and refugia-0.1.0/docs/data/README.md (hatchling's default sdist take; only [tool.hatch.build.targets.wheel] is configured, pyproject.toml:47-48).\n\nBut it is not a blocker and it is not independent. `gh api .../activity` already hands out b38ccb8, the pre-rewrite tip, without any help from this file — so removing the SHA from the ledger removes a convenience, not the exposure. Rated blocker it competes for attention with the finding that actually matters, and worse, the finder's remedy (\"strip the pre-rewrite object ids\") would look like progress while leaving the leak fully reachable. It is real as tidy-up and as sdist hygiene; the disclosure risk is entirely downstream of the previous finding.", "paths: docs/data/findings.jsonl:3, docs/data/findings.jsonl:11, pyproject.toml:47-48"], "updated": "2026-09-07", "notes": "Do the remote fix first; it moots the pointer. Then, separately and for its own sake: add `[tool.hatch.build.targets.sdist] exclude = [\"docs/data\"]` (or `include` the paths you actually want) to pyproject.toml, since an internal audit ledger with 62 rows of unfixed-vulnerability prose is not something `pip download refugia` should carry. Stripping cfb6988/67efc6a from fnd-003 and fnd-011 is optional cosmetics once the remote is clean — do not treat it as the mitigation.", "batch": "publish-the-repo"}
{"id": "fnd-078", "kind": "finding", "title": "A failed source leaves a saved dataset indistinguishable from a successful fetch", "status": "open", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "security-quality", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Verified end to end. cli.py:28-59 _collect seeds `values` from `existing` and `continue`s past a raising source; cli.py:185-192 constructs Dataset with built_at=today and oldest_response unconditionally; `failures` is only printed (cli.py:88-103) and Dataset (store/dataset.py:24-30) has no field for it. The finder's subtler point also holds: oldest_response cannot cover the gap, because fetch is cache-first — a source only raises when its cache entry is absent, so the carried-over values have no cache entry to date them, and template.html:2819-2824 then prints 'Built …; oldest cached response …' over numbers that were not refetched. Not a blocker (nothing is leaked and no user is misled about a place's identity), but it is precisely the case fnd-019 was opened for.", "paths: /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/cli.py, /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/store/dataset.py, /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/artifact/template.html"], "updated": "2026-09-07", "notes": "Add an optional `not_refreshed: tuple[str, ...]` (metric keys) to Dataset alongside built_at/oldest_response — optional keeps old files loadable, as built_at already does — populate it in cli.py:fetch from _collect's failures by expanding each failed source's metric keys, name it in _report_coverage, and have template.html's PROVENANCE line append 'N metric(s) not refreshed on this build'. Extend tests/test_provenance.py and tests/test_cli_resilience.py, which already exercises the carry-over.", "batch": "upstream-resilience"}
{"id": "fnd-079", "kind": "finding", "title": "A metric's category reaches markup unescaped at four sites — and so do metric.key and mapMetric, which the finder missed", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "trivial", "lens": "security-quality", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced exactly: template.html:1436 writes data-cat=\"${esc(c)}\" and the next four uses are raw — 1438 `<summary>${c}`, 1439 `id=\"cnt-${c}\"`, 1446 `data-cat=\"${c}\"` on the checkbox, 1451 `data-cat=\"${c}\"` on the range — all concatenated into #weights.innerHTML at 1433. declarative.py:23 DISPLAYED and :77-79 reject only `<`/`>`, so `\"category\": \"air\\\" onfocus=… autofocus=\\\"\"` passes _validate and breaks the attribute. esc() itself (template.html:1119-1125) does escape quotes, so wrapping is sufficient. BUT the finder's headline claim — \"I enumerated all 40 distinct ${…} expressions … ${c} is the only unescaped data-bearing one left\" — is false. Two more spec-derived values reach markup raw: template.html:1450 `data-k=\"${m.key}\"` on the range input (the sibling checkbox at 1446 uses esc(m.key)), and template.html:1982 `aria-label=\"US counties by ${mapMetric}\"`, where mapMetric is a metric key taken from the select at :1644. `key` is in DISPLAYED and is likewise only screened for angle brackets. Separately I checked the finder's dismissal of the fmt() path and agree it is now closed by shape: fmt (template.html:1205-1212) no longer emits the unit at all.", "paths: /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/artifact/template.html, /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/metrics/sources/declarative.py", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Wrap six sites, not four: esc(c) at template.html:1438, 1439, 1446, 1451; esc(m.key) at 1450; esc(mapMetric) at 1982. Then widen declarative.py:77-79 so DISPLAYED also rejects `\"` and `'`, and add a stricter charset check for `key` specifically (it is used as an element id and a data attribute: `^[a-z0-9_]+$` is all it ever needs to be). fnd-021's evidence line \"every data-bearing interpolation into markup now passes through it\" and fnd-037's \"closes fnd-021's residual fmt() path\" should be amended in docs/data/findings.jsonl — the pair does not yet make accepting an outside spec safe. FIXED: Six sites wrapped in esc(): the category into three attributes, m.key into a data attribute, mapMetric into an aria-label, and the ask chips. _validate now rejects quotes as well as angle brackets in every displayed field, and constrains key to [a-z0-9_]+ since it becomes an element id.", "batch": ""}
{"id": "fnd-080", "kind": "finding", "title": "Any `error` key in a LANDFIRE 200 body is classified as permanent OutsideCoverage — real, but pre-existing, not caused by the Retry rewrite", "status": "open", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "security-quality", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["The defect is real and reproduced: landfire_vegetation.py:248-249 raises OutsideCoverage on any `error` key without inspecting body['error']['code']; OutsideCoverage is not in Retry's transient set (retry.py:53-57), fetch() files it in self._outside (landfire_vegetation.py:150-152), and cli.py:66-67 prints the benign \"N place(s) outside this layer's coverage\" rather than cli.py:69's \"failed after retries\". An ArcGIS `{\"error\":{\"code\":500,…}}` is therefore recorded as a permanent geographic gap. But the finder's causal claim — \"The Retry rewrite made this worse, not better: the old broad loop would at least have retried some of these\" — is wrong. `git show 63ac87c^:src/refugia/metrics/sources/landfire_vegetation.py` shows the pre-rewrite loop caught only `(httpx.HTTPError, OSError)` with the comment \"OutsideCoverage is deliberately not caught below: it is permanent\". Behaviour is identical before and after, so this is a pre-existing gap, not a new defect introduced by fnd-038, and it does not belong on fnd-038's ledger.", "paths: /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/metrics/sources/landfire_vegetation.py"], "updated": "2026-09-07", "notes": "In _request_once (landfire_vegetation.py:246-249), branch on the ArcGIS code: treat 400-class codes and the LF2025-style rejection CLAUDE.md documents as OutsideCoverage, and raise an httpx-transient error (or add a named error to Retry's `also_transient`) for 5xx/'busy'/'timeout'. Anything unrecognised should go to `self._failures`, not `self._outside`, so the CLI reports it under the line that says something failed. Add two tests alongside the existing landfire tests: a 500-shaped error body retries, a coverage rejection does not.", "batch": "upstream-resilience"}
{"id": "fnd-081", "kind": "finding", "title": "The `</script>` breakout guard in the artifact builder has no test: deleting it leaves all 201 tests passing", "status": "fixed", "severity": "P1", "release_gate": "important", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["src/refugia/artifact/builder.py:62-63 is the single line standing between a value in the dataset and script-tag breakout in the published page: `# `</script>` inside embedded JSON would close the host tag early.` / `encoded = json.dumps(payload, separators=(\",\",\":\")).replace(\"</\", \"<\\\\/\")`. I removed the `.replace(\"</\", \"<\\\\/\")` call entirely in a scratchpad copy and ran the full suite: `201 passed in 0.30s`. Nothing noticed. This is the guard added by commit a8e7968 (\"escape what the page interpolates, and refuse markup at ingest\"), and only the *other* half of that commit is tested — mutating the ingest check at metrics/sources/declarative.py:78 to `if False:` correctly fails 6 tests in tests/test_declarative.py. So the cheap, reviewed-input half is guarded and the runtime-data half is not. It matters more than the ingest half, because place names and metric labels reach the payload from Census and upstream CSVs, which are not reviewed. tests/test_builder.py's five tests (lines 8-35) check only doctype, charset, viewport and head/body partitioning; nothing in the repo asserts anything about the JSON encoding. The finder ran 13 mutants and did not probe this file.", "paths: src/refugia/artifact/builder.py, tests/test_builder.py", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Add to tests/test_builder.py: build a page from a Dataset whose place name (or a metric label) contains the literal `</script><img src=x onerror=alert(1)>`, then assert `\"</script>\" not in written_html.split(PLACEHOLDER_REGION)[...]` — concretely, assert the raw substring `</script>` appears exactly once in the output (the page's own closing tag) and that `<\\/script>` appears in the embedded payload. That single assertion kills the mutant. While there, consider asserting the same for `<!--` and `<script`, which `.replace(\"</\", ...)` does not cover. FIXED: Two tests in test_builder.py: the payload encoding directly, and an end-to-end build from a place named `</script><img src=x onerror=alert(1)>` asserting the written file closes exactly twice. Mutation-checked -- deleting the replace in builder.py fails it.", "batch": ""}
{"id": "fnd-082", "kind": "finding", "title": "The conformance vectors guard weights and coverage; the page silently ignores `normalization` and `criteria`, which the guide documents as user choices", "status": "open", "severity": "P1", "release_gate": "important", "effort": "medium", "lens": "tests", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced every element. `grep -c 'criteria\\|normalization' src/refugia/artifact/template.html` -> 0. builder.py:58 `payload[\"profile\"] = profile.to_dict()` ships both fields; profile.py:52-67 emits them; engine.py:31 `Normalizer(method=profile.normalization)` and engine.py:44 `self._apply_filters(...)` honour them on the Python side. tests/test_scoring_conformance.py:62-66 builds `Profile(case[\"name\"], weights, min_coverage=...)` only, and all 12 vectors carry exactly {name, metrics, weights, min_coverage, places, values, expect}. I also confirmed the CLI does not pre-filter for the page: ArtifactBuilder.build takes `dataset.places` whole. STRONGER than the finder stated: docs/guide/reading-the-numbers.md:3-4 asserts \"`score` in the CLI, `FIT` on the page, is a 0-100 percentile rank _among the places that survived your filters_\" — both halves of that sentence are false for the page under a profile with `minmax` or with `criteria`.", "paths: src/refugia/artifact/template.html, src/refugia/artifact/builder.py, src/refugia/scoring/profile.py, tests/conformance/vectors.json, tests/test_scoring_conformance.py, docs/guide/reading-the-numbers.md, docs/guide/writing-a-profile.md"], "updated": "2026-09-07", "notes": "Cheapest honest fix, and the one I would take before going public: make ArtifactBuilder.build raise when handed a profile whose `normalization != \"percentile\"` or whose `criteria` are non-empty and unapplied, with a test in tests/test_builder.py for each; and correct docs/guide/reading-the-numbers.md:3-4 so it does not claim the page filters. The larger fix (teach the JS kernel minmax, seed `filters` at template.html:1154 from `D.profile.criteria`, and add vectors that vary `normalization` — which requires threading it through `scoreAll`'s signature at template.html:1268) is the right end state but is medium work. Do not widen the vectors without also changing the page: that only documents the divergence.", "batch": "scoring-parity"}
{"id": "fnd-083", "kind": "finding", "title": "A misspelled `criteria` field returns an empty ranking with no message; the guide promises otherwise", "status": "open", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced end to end. A copy of profiles/example.json with criteria field \"populaton\" prints only the header row and exits 0; the same profile with weight key 'juniper_covr' raises KeyError: \"profile weights an unknown metric 'juniper_covr'; run `refugia metrics` for the registered keys\". So writing-a-profile.md:34 (\"A key that is not in it stops the run and says so\") holds for weights only. The finder's line numbers are wrong and were not read from the files — engine.py is 123 lines (they cite :155-162 and :191-200) and criterion.py is 33 lines (they cite :107-110). The real code is engine.py:43 (the KeyError), engine.py:68 (criterion.accepts(self._observe(...))) and engine.py:75-83 (_observe: hasattr/getattr on the Place, else values.get(field, {}).get(place.fips), else None). Strengthening evidence the finder missed: src/refugia/ask/planner.py:78 already validates criterion fields against `known | {\"population\",\"state\",\"cbsa_type\"}` — the LLM path is guarded and the human path is not.", "paths: src/refugia/scoring/engine.py:60-83, src/refugia/ask/planner.py:78, docs/guide/writing-a-profile.md:34-41"], "updated": "2026-09-07", "notes": "Reuse the vocabulary planner.py:78 already computes: in ScoringEngine.rank, before filtering, raise the same style of KeyError for any criterion.field that is neither a registry key nor an attribute of Place — `raise KeyError(f\"profile filters on an unknown field {field!r}; run `refugia metrics` for the registered keys, or use population/state/name/lat/lon/fips/metro/cbsa_type\")`. Also amend writing-a-profile.md:39-40 to say an unrecognised `field` matches nothing, so the mistake empties the ranking rather than erroring, until the check lands.", "batch": "scoring-parity"}
{"id": "fnd-084", "kind": "finding", "title": "The docs say only `fetch` touches the network; the first `publish` downloads four more sources and resamples LANDFIRE per city", "status": "open", "severity": "P1", "release_gate": "important", "effort": "small", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["The finder did not look at what publish does. command-line.md:3-4 and :13-14 and README.md:112 all say \"`fetch` is slow and network-bound; everything else reads the saved dataset, so re-weighting costs nothing\", and \"What a fetch costs\" (command-line.md:58-62) attributes the whole cache to fetch. But cli.fetch only runs the registry's metric sources, while ArtifactBuilder.build (builder.py:48-52) constructs CountyShapes, CityMarkers and CityProfile, which fetch four further endpoints on first use: the us-atlas county topojson from a jsdelivr CDN (geometry.py:9,:39), the Census city population CSV and the 2024 Gazetteer zip (city_markers.py:12-16,:137-142), and the CDC PLACES place release, paged 20,000 rows at a time (city_profile.py:14,:120-140). Worse, CityProfile._vegetation_cover (city_profile.py:69-88) calls LandfireVegetationSource.cover_at once per mapped city across a thread pool — thousands of live samples. All of it is cached afterwards, so the claim is true from the second publish on and false for the first, which is exactly the run a new reader following the quickstart makes. A user who fetches, disconnects, and then publishes gets a failure the docs told them could not happen.", "paths: docs/guide/command-line.md:3-4, docs/guide/command-line.md:13-14, docs/guide/command-line.md:58-62, README.md:112-114, src/refugia/artifact/builder.py:47-52, src/refugia/artifact/geometry.py:9,:39, src/refugia/artifact/city_markers.py:12-16,:137-142, src/refugia/artifact/city_profile.py:14,:59-92"], "updated": "2026-09-07", "notes": "Amend command-line.md:3-4 to \"`fetch` is slow and network-bound, and the first `publish` is too — it downloads the county outlines, the city markers and the CDC place release and resamples land cover around each town, then caches all of it. `rank` and every later `publish` read the saved dataset.\" Add a line to \"What a fetch costs\" saying which part of data/cache/ the first publish adds, and drop the same qualifier into README.md:112-114.", "batch": "upstream-resilience"}
{"id": "fnd-085", "kind": "finding", "title": "CONTRIBUTING describes a dangling symlink a fresh clone does not have, and calls the hooks the same checks CI runs", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "contributor-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Cloned the repo into /tmp/fclone: no .pre-commit-config.yaml at all, and `git ls-files | grep pre-commit` is empty (.gitignore:7-8 ignores it; the working copy's is a symlink into /nix/store written by devenv). So CONTRIBUTING.md:34-37's \"You will see a broken symlink in a fresh clone\" is false for the audience it addresses. Second sentence also false: devenv.nix:29-70 configures alejandra, deadnix, prettier, check-json, check-yaml, check-added-large-files, check-merge-conflicts, detect-private-keys, end-of-file-fixer, trim-trailing-whitespace and the commit-msg convention script, none of which CI runs, and no pytest hook, which CI does. One thing the finder did not check that I did: CONTRIBUTING.md:29-32's other claim, that the node-dependent conformance test \"always runs in CI\", does hold — tests/test_scoring_conformance.py:115-119 fails rather than skips when `CI` is set.", "paths: CONTRIBUTING.md:34-37, .gitignore:5-8, devenv.nix:29-70", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Rewrite CONTRIBUTING.md:34-37: the symlink appears only after a devenv shell entry, so a plain clone will not have one; and the hooks are a superset of CI on formatting (prettier, alejandra, whitespace) and a subset on tests (no pytest). If prettier's markdown/YAML formatting is expected of outside PRs, say so or add it to ci.yml; today a fork is green in CI and then reformatted on merge. FIXED: CONTRIBUTING says a plain clone has no .pre-commit-config.yaml at all, and that the hooks are a superset on formatting and a subset on tests.", "batch": ""}
{"id": "fnd-086", "kind": "finding", "title": "The bug form asks for `refugia --version`, which the CLI does not implement", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "contributor-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Ran it: `devenv shell -- uv run refugia --version` -> \"Error: No such option: --version\". .github/ISSUE_TEMPLATE/bug.yml:36 asks for it, hedged with \"if you have it\". src/refugia/cli.py has no version callback. Nice-to-have is the right gate — no reporter is blocked, the form also asks for the commit and the install method (bug.yml, id: install).", "paths: .github/ISSUE_TEMPLATE/bug.yml:33-38, src/refugia/cli.py:22-24", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Add a Typer callback with `--version` printing `importlib.metadata.version(\"refugia\")` and exiting; keep the form unchanged. Worth pairing with the tag fix above, since the version string is only useful once it identifies distinct code. FIXED: The bug form asks for the commit rather than an option the CLI does not implement.", "batch": ""}
{"id": "fnd-087", "kind": "finding", "title": "adding-a-spec.md says a rejected spec is merely absent from `refugia metrics`; in fact one bad spec aborts every command with a traceback", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "contributor-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced independently with a different defect than the finder used. A temp root with specs/bad.json carrying `\"direction\": \"lower_is_better\"` and `refugia metrics --root /tmp/spectest` printed a full Rich traceback through DeclarativeSource.__init__ -> _validate ending `ValueError: direction must be higher_better or lower_better`, and no metric list. Because workspace.build_registry (workspace.py:67-83) constructs every DeclarativeSource eagerly, `fetch`, `rank`, `publish` and `ask` all die the same way. The message names the failing field but not the file. The internal contradiction is also real: workspace.py:70-73 says \"Specs are registered last so a written adapter always wins a key collision\", while workspace.py:59-62 and MetricRegistry.register (metrics/registry.py:20-30) both raise on a duplicate key — nothing wins.", "paths: docs/guide/adding-a-spec.md:132-133, src/refugia/workspace.py:56-83, src/refugia/metrics/registry.py:20-30"], "updated": "2026-09-07", "notes": "In build_registry, wrap `DeclarativeSource.from_file(spec, ...)` so a ValueError is re-raised as `ValueError(f\"{spec}: {error}\")` — cheap, names the file, and keeps the loud failure. Then rewrite adding-a-spec.md step 2 to match: an invalid spec stops every command with the validator's message naming your file. Fix the workspace.py:70-73 docstring to say a duplicate key raises rather than resolving in the adapter's favour.", "batch": "prose-guard"}
{"id": "fnd-088", "kind": "finding", "title": "ci.yml declares no `permissions:`, so the workflow token takes the repository default once the repo is public", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "contributor-docs", "cycle": "cyc-release-audit-2", "finding_kind": "repo-furniture", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["The workflow has `on: push/pull_request`, a concurrency block and a single `check` job, and no `permissions` key at any level. Nothing in it needs write access — it checks out, syncs, lints and tests. Whether GITHUB_TOKEN ends up read-only depends on an organisation/repository setting no file in the repo records, which is the kind of thing worth pinning before a repo becomes public and starts receiving fork PRs.", "paths: .github/workflows/ci.yml:16-19", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Add `permissions: contents: read` at the top level of .github/workflows/ci.yml. One line, no behaviour change, and it makes the least-privilege choice visible in the repo rather than in a settings page. FIXED: ci.yml declares permissions: contents: read.", "batch": ""}
{"id": "fnd-089", "kind": "finding", "title": "docs/data/README.md miscounts the file it documents", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "contributor-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Counted with a JSON parse: 66 rows, statuses Counter({fixed: 48, declined: 13, parked: 3, open: 2}). docs/data/README.md:84 says \"62 rows\" and :49 says \"Twelve rows are `declined`\". The finder's structural checks I re-ran spot-wise also hold (ids unique, no fixed row with empty evidence). Two further miscounts in the same paragraph that the finder missed, listed separately below.", "paths: docs/data/README.md:49,84-90, docs/data/findings.jsonl"], "updated": "2026-09-07", "notes": "Update the counts (66 rows, 13 declined) and add the integrity test §4 already promises: a dozen lines asserting the README's stated counts equal the parsed file's, plus the id/reference invariants, is cheaper than the paragraph explaining why it is missing.", "batch": "prose-guard"}
{"id": "fnd-090", "kind": "finding", "title": "docs/data/README.md §5 also says \"seven lenses\" while listing eight, and \"refuted six\" against four REFUTED rows", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "contributor-docs", "cycle": "cyc-release-audit-2", "finding_kind": "doc-accuracy", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Same paragraph the finder flagged for the row counts, two further errors it did not check. The text reads \"Seven lenses found; each lens's findings were then adversarially verified ... and refuted six\", then lists eight lens names. Parsing the file: the `lens` field takes eight distinct values — data-licensing 12, critique 10, security-quality 8, user-docs 8, contributor-docs 7, personal-data 7, reproducibility 7, tests 7 — and only four rows are titled \"REFUTED — ...\" (of 13 declined).", "paths: docs/data/README.md:84-90"], "updated": "2026-09-07", "notes": "Fix in the same edit as the row counts: eight lenses, and either say how many were refuted from the data or drop the number. If the integrity test suggested in §4 gets written, assert the lens count and the declined/refuted counts against the parsed file rather than restating them in prose.", "batch": "prose-guard"}
{"id": "fnd-091", "kind": "finding", "title": "new-metric-source.yml applies a `metric` label that may not exist in the repository", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "contributor-docs", "cycle": "cyc-release-audit-2", "finding_kind": "repo-furniture", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["bug.yml:3 uses `labels: [\"bug\"]`, which every GitHub repo has by default; new-metric-source.yml:3 uses `labels: [\"metric\"]`, which is not a default label. I could not verify the repository's label set from here (private repo, offline audit), so this is a check to run rather than a confirmed defect: if the label is absent, GitHub silently files the issue without it and the triage query the form was built for returns nothing.", "paths: .github/ISSUE_TEMPLATE/new-metric-source.yml:3"], "updated": "2026-09-07", "notes": "Create the `metric` label in the repository (or change the template to an existing one) before publishing, and confirm by opening one test issue from each form.", "batch": "packaging"}
{"id": "fnd-092", "kind": "finding", "title": "'The repo vendors no upstream data' is not exact — a tracked screenshot shows CHR&R values for two named counties", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["I opened docs/screenshot-compare.jpg myself and read the same five CHR&R pairs for Yankton County SD vs Cook County IL: air pollution 6.1/10.8, poor mental health days 4.2/5.2, poor or fair health 12.7/17.5, social associations 15.0/7.2, life expectancy 79.2/77.4. Both jpgs are tracked. So fnd-056's evidence line is indeed a grep that structurally could not see this. Two corrections. The exposure is slightly larger than 'twelve data points': docs/screenshot-map.jpg is a national choropleth of composite fit over 1,906 counties, partly derived from the CHR&R metrics — still not recoverable as values, still de minimis. And the finder's remedy is largely already satisfied: README.md:282-284 reads 'The code is MIT — see LICENSE. The data is not the code's to license: refugia ships none of it', which is the scoping line it asks for. Only the findings-row evidence needs amending. Nothing here blocks release, and I would not change the screenshots either. (The compare panel's baseline is Cook County, which is profiles/example.json's home_fips, so no personal home county is disclosed.)", "paths: docs/screenshot-compare.jpg, docs/screenshot-map.jpg, docs/data/findings.jsonl (fnd-056 evidence)"], "updated": "2026-09-07", "notes": "Amend fnd-056's first evidence line to 'the repo vendors no upstream dataset; two documentation screenshots show a dozen CHR&R-derived values for two counties and a national composite choropleth, with the page's credit line visible'. No code, licence or README change needed.", "batch": "attribution"}
{"id": "fnd-093", "kind": "finding", "title": "A dataset saved before the citation field existed loads clean and publishes a page with no citation", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["The mechanism is real and I confirmed it: dataset.py:81 does `Metric(**m)`, metric.py:32-33 defaults citation and terms_url to '', and the two lines below give built_at an explicit `payload.get(..., \"\")` with a comment about stale files loading — so the provenance case was handled and the attribution case was not. But the stated consequence is inflated. v0.1.0 is the first tag (`git tag` -> v0.1.0), data/dataset.json and out/ are gitignored (.gitignore), so no pre-citation dataset exists anywhere but the author's machine, and the author's own data/dataset.json already carries citations on 6 of its 21 metrics (I parsed it: home_value plus the five CHR&R keys). This is a forward-looking hazard — the next field added to Metric degrades the same silent way — not 'the one path that produces a shared page carrying CHR&R content with no citation'.", "paths: src/refugia/store/dataset.py:78-86, src/refugia/metrics/metric.py:32-33, src/refugia/cli.py:127-131"], "updated": "2026-09-07", "notes": "Metric declarations are code, not data: on load, re-bind each metric's descriptive fields from the registry by key and keep only the values from the file, so a stale dataset cannot carry stale attribution or a stale radius string. Cheaper alternative: have publish warn when a metric key that the registry says carries a citation loads without one.", "batch": "attribution"}
{"id": "fnd-094", "kind": "finding", "title": "DATA_SOURCES.md's opening summary says only two sources carry obligations — but us-atlas is a third, and it is the one the audit called the only outright breach", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "doc-accuracy", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["DATA_SOURCES.md:9-12 reads 'Two carry real obligations… The rest are US federal works, public domain, where attribution is courtesy.' Three of its own sections contradict that: us-atlas is ISC (Michael Bostock, not federal) and its own section says the licence 'conditions redistribution on retaining the copyright notice' — a condition, not courtesy, and per fnd-060 the only licence-condition breach the audit found; Wildfire Risk to Communities is explicitly recorded as non-federal-partnered and 'inferred, not confirmed'; and Google Fonts is absent entirely. The finder caught the LANDFIRE/POWER half of this contradiction but not the us-atlas half, which is the stronger one because it is a binding condition rather than a request. A reader who reads only the summary — which is what a summary is for — concludes the page footer needs two credits when it needs three.", "paths: DATA_SOURCES.md:9-12, DATA_SOURCES.md:150-160 (us-atlas section), DATA_SOURCES.md:120-130 (Wildfire Risk section)", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Rewrite DATA_SOURCES.md:9-12 as 'Three carry obligations: CHR&R (personal/non-profit only, required citation), Zillow (required attribution) and us-atlas (ISC, notice must travel with the page). Two positions are inferred. The rest are US federal works.' FIXED: The summary says three obligations and distinguishes the ISC condition from a licence scope and an attribution request.", "batch": ""}
{"id": "fnd-095", "kind": "finding", "title": "LANDFIRE and NASA POWER both request citation, DATA_SOURCES.md records it, and neither citation reaches the page", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["landfire_vegetation.py:84-116 declares three metrics with source strings and no citation/terms_url; nasa_power_climate.py's four metrics likewise (grep for citation across src returns only declarative.py, zillow_home_value.py and the five CHR&R specs). data/dataset.json confirms exactly 6 of 21 metrics carry a citation. DATA_SOURCES.md:68-71 records LANDFIRE's citation model and :106 records 'NASA requests citation', while :12 says the rest are 'public domain, where attribution is courtesy' — the file contradicts itself. Requested is not required, so nothing is breached.", "paths: src/refugia/metrics/sources/landfire_vegetation.py:84-116, src/refugia/metrics/sources/nasa_power_climate.py (metrics property), DATA_SOURCES.md:12,68-71,106", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Add LANDFIRE's citation-model string (with terms_url https://www.landfire.gov/data/citation) and NASA POWER's requested citation to those metrics, and reword DATA_SOURCES.md:12 so 'attribution is courtesy' does not contradict the two entries that record a requested citation. FIXED: The same paragraph records that LANDFIRE and NASA POWER both request citation.", "batch": ""}
{"id": "fnd-096", "kind": "finding", "title": "Nothing forces an attribution-owing source to arrive with a citation; the check only fires once one has been written", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["The mechanical claim holds: tests/test_specs.py:52-57 is guarded by `if payload.get(\"citation\")`, so a spec with neither field passes; zillow_home_value.py:42-43 is the only adapter citation and no test asserts an adapter has one; metrics/city_only.py CITY_ONLY is a hand-written dict with no citation/terms_url, injected at builder.py:57-58 without passing through Metric. But the finder overstates the exposure two ways. (a) CITY_ONLY's only content is CDC PLACES, federal public domain, so nothing currently uncited owes a citation — the gap is future-only. (b) The obligation is not left to memory: CONTRIBUTING.md:87-93, docs/guide/adding-a-spec.md:105-106 and .github/ISSUE_TEMPLATE/new-metric-source.yml (a *required* 'Terms of use, and whether you read them or inferred them' field) all put it in front of a contributor and a reviewer. What is wrong is the prose in DATA_SOURCES.md:172-175 and template.html:2800-2805 asserting a structural guarantee ('cannot be left out of this list by someone who did not know it owed one') that only a reviewer provides.", "paths: DATA_SOURCES.md:172-175, src/refugia/artifact/template.html:2800-2805, tests/test_specs.py:52-57, src/refugia/metrics/city_only.py:5-24, src/refugia/artifact/builder.py:57-58"], "updated": "2026-09-07", "notes": "Soften the two prose claims to what is true — 'a citation written on a metric travels into the page and cannot be dropped later; whether a new source gets one is a review question, and the new-source issue template asks it'. Optionally make CITY_ONLY entries Metric instances so one type carries attribution for every column the page shows, and extend the spec check to run over Workspace(root).build_registry().metrics so written adapters are covered by the same assertion as specs.", "batch": "attribution"}
{"id": "fnd-097", "kind": "finding", "title": "The NASA POWER pool has no throttle, and 429 is retried on a fixed backoff that ignores Retry-After", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["nasa_power_climate.py:49-55 takes workers=6 and constructs a Retry but no Throttle; there is no `throttle` reference anywhere outside landfire_vegetation.py (grep over src). Cache mtimes reproduce the rate: power-* n=212, span 8.5s, 24.8 req/s — higher instantaneous than the throttled LANDFIRE pool. One scope correction: the Retry-After gap is not POWER-specific. retry.py:14 puts 429 in RETRY_STATUSES for every source that uses the shared Retry (CDC PLACES, the declarative specs, LANDFIRE, Zillow), and retry.py:71 sleeps 1.5·2^n regardless of what the service asked for, so the remedy belongs in Retry, not in one adapter. Volume is genuinely small, so nice-to-have is right.", "paths: src/refugia/metrics/sources/nasa_power_climate.py:49-55, src/refugia/retry.py:14,60-73, src/refugia/metrics/sources/sampling.py:22", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Honour Retry-After inside Retry.run when the error is an HTTPStatusError carrying that header (it then applies to every source at once), and give the POWER source a Throttle. If a project-wide rate policy is wanted, move min_interval off Sampling — which only the raster source reads — and have Workspace construct the Throttle and hand it to each pooled source. FIXED: NasaPowerClimateSource takes a Throttle at min_interval, waited before each request, so its six-worker pool no longer opens without a floor.", "batch": ""}
{"id": "fnd-098", "kind": "finding", "title": "The one call site exempted from the User-Agent rule can be pointed at any host from the command line", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["tests/test_outbound_requests.py:14-16 exempts ask/planner.py with the reasoning 'The planner talks to an Ollama on the reader's own machine, not to a public endpoint that has to tolerate every fork of this project.' That is a default, not a property: cli.py:279 exposes `--host` (default http://localhost:11434) and passes it straight to QueryPlanner, and planner.py:124 POSTs to f'{host}/api/chat' with no headers argument. Pointed at a hosted OpenAI-compatible or shared Ollama endpoint — the obvious thing a user without a 30B-capable machine does — it becomes an unidentified outbound request carrying county names and metric values from the same project every other request identifies. The exemption is also the seam through which a future contributor can add an outbound call the guard will never see.", "paths: tests/test_outbound_requests.py:14-16, src/refugia/ask/planner.py:58,124, src/refugia/cli.py:279-284"], "updated": "2026-09-07", "notes": "Send headers={'User-Agent': USER_AGENT} at planner.py:124 and delete the EXEMPT set; the header costs nothing against a local Ollama and removes the one hole in the rule. If the exemption is kept, reword its comment to say the default is localhost and the header is skipped deliberately.", "batch": "attribution"}
{"id": "fnd-099", "kind": "finding", "title": "`publish`'s sharing note counts attribution from metrics, so the us-atlas ISC condition — the only binding one — is never mentioned to the person about to share", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["cli.py:127 computes `cited = {m.citation for m in dataset.metrics if m.citation}` and prints '2 source(s) require attribution and their citations travel in the page footer.' The comment above it justifies counting rather than hardcoding, which is sound for metrics — but the page also embeds geometry derived from ISC-licensed us-atlas, whose notice is a redistribution condition, and that is a hand-written CREDITS block, not a metric, so it is structurally invisible to the count. The one moment that reaches the person about to redistribute understates what travels with the file by exactly the item fnd-060 was opened for. fnd-056 was closed on this note being sufficient.", "paths: src/refugia/cli.py:127-133, src/refugia/artifact/template.html:2806-2810", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Add one fixed sentence after the counted line: 'The page also carries the ISC notice for the county outlines (us-atlas), which its licence requires you to keep.' Counting is right for metrics; the non-metric credits are a constant and should be stated as one. FIXED: The sharing note adds a fixed sentence for the county outlines, which are not a metric and so could not be counted.", "batch": ""}
{"id": "fnd-100", "kind": "finding", "title": "fonts.googleapis.com is the one endpoint the published page contacts that DATA_SOURCES.md does not list", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["template.html:2-6 preconnects to fonts.googleapis.com and fonts.gstatic.com and loads a css2 stylesheet for Fraunces, IBM Plex Mono and Source Sans 3. DATA_SOURCES.md has no entry for it, while README.md:294-299 discloses it plainly. No licence obligation attaches (open-licensed families, linking is not redistribution), so this is consistency in the file a reader is sent to for the complete list.", "paths: DATA_SOURCES.md, src/refugia/artifact/template.html:2-6", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "One short DATA_SOURCES entry: Google Fonts, both hosts, what is fetched, that the families are open-licensed and the page falls back to system faces offline, pointing at README's disclosure. FIXED: DATA_SOURCES names fonts.googleapis.com as the one contacted endpoint that supplies no data.", "batch": ""}
{"id": "fnd-101", "kind": "finding", "title": "terms_url is collected, validated and shipped in the payload, and never shown to a reader", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "data-licensing", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["grep for terms_url in src/refugia/artifact/template.html returns nothing; template.html:2812 builds CITATIONS from `M.map((m) => m.citation)` alone. dataset.py:68 serialises it and tests/test_specs.py:44-50 validates its scheme, so it exists end to end except at the point of use. DATA_SOURCES.md:172-173 says 'A metric carries its own citation and terms_url, and the published page builds its citation list from them', which is only half true. Mitigation: the CHR&R citation string itself contains 'licensed for personal and non-profit use' and the bare domain, so the obligation is stated even without the link.", "paths: src/refugia/artifact/template.html:2812, DATA_SOURCES.md:172-173"], "updated": "2026-09-07", "notes": "Render each citation as an anchor to its terms_url when present (esc the href, target=_blank rel=noopener as the CREDITS block already does), or drop 'and terms_url' from the DATA_SOURCES sentence.", "batch": "attribution"}
{"id": "fnd-102", "kind": "finding", "title": ".gitignore's profiles/*.json misses a subdirectory and any non-.json profile", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "personal-data", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced exactly. `git check-ignore -v profiles/x.json` -> `.gitignore:27:profiles/*.json`; `git check-ignore -v profiles/mine/secret.json` and `profiles/personal.json5` -> no match, exit 1. profiles/ currently holds exactly one file, example.json, and `git check-ignore` correctly does not ignore it (the negation on .gitignore:28 works). Severity is right at nice-to-have: the guide and README only ever show the flat `profiles/<name>.json` form, so the realistic path is covered, and the sdist carries only profiles/example.json regardless.", "paths: .gitignore:26-28", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Replace `profiles/*.json` with `profiles/**`, keeping `!profiles/example.json` on the next line. Verified this shape works in git: `profiles/**` does not match the `profiles/` directory itself, so the negation still re-includes the one tracked file, while `profiles/mine/secret.json` and `profiles/jason.yaml` both become ignored. FIXED: profiles/** with the example negated, so a subdirectory or a non-.json profile is covered too.", "batch": ""}
{"id": "fnd-103", "kind": "finding", "title": "The primary test fixture places \"Alpha, Oregon\" at 44.0, -121.0 — in or on the edge of the author's home county", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "personal-data", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["tests/conftest.py:43 reads exactly as quoted: `Place(\"00001\", \"Alpha\", \"Oregon\", 44.0, -121.0, 100_000, Cbsa(\"1\", \"Alpha, OR\", \"metro\"))`. 44.0N/-121.0W is central Oregon, in or immediately adjacent to Deschutes County (Bend is 44.06/-121.31). fnd-050's declining evidence is \"conftest.py uses Oregon/Idaho/Maine/Ohio with synthetic FIPS 00001-00004 and NATO-alphabet names\" — it addresses names and FIPS only, so the coordinates were genuinely never examined. The finder's own hedging is right and I share it: round numbers, one weak signal, nothing else corroborates. It is a tell only to someone who already knows the answer.", "paths: tests/conftest.py:43", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Change the coordinates to any plausible CONUS pair; I confirmed nothing asserts on 44.0/-121.0 — the only other reference to the fixture geography is tests/test_geometry.py, which uses its own coordinates. CORRECTION TO THE FINDER'S REMEDY: do NOT change Alpha's state without also editing tests/test_engine.py:62, which is `Criterion(\"state\", \"not_in\", (\"Oregon\", \"Idaho\"))` and depends on Alpha being in Oregon. Moving the coordinates alone is the zero-risk edit. FIXED: The fixture moved to 39.0, -98.0. Its state stays Oregon because test_engine filters on it.", "batch": ""}
{"id": "fnd-104", "kind": "finding", "title": "The v0.1.0 annotated tag object carries jasonrisch@gmail.com as tagger, a surface the ledger's sweep never names", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "personal-data", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["The finder's fnd-045 check covered tracked files and `git log` authorship, and concluded the email survives \"in git log authorship only\". That is one surface short. `git cat-file -p 1a9adc6d9a23d4f00595e5c4f5e48e68a06dc7c5` gives `tagger Jason Risch <jasonrisch@gmail.com> 1788792722 -0700`, and `git ls-remote origin` confirms refs/tags/v0.1.0 is pushed. Keeping the real name and email in commit authorship is fnd-003's explicit, defensible owner decision and I am not reopening it — the point is narrower: the record says \"git log authorship\" and the tag object is a second place carrying the same identity, so anyone later deciding to scrub identity would miss it. Not a release blocker under the decision as it stands.", "paths: refs/tags/v0.1.0 (tag object 1a9adc6d9a23d4f00595e5c4f5e48e68a06dc7c5), docs/data/findings.jsonl (fnd-003, fnd-045)"], "updated": "2026-09-07", "notes": "No action needed if the owner's decision to keep public commit identity holds — just amend fnd-045's evidence to say \"commit authorship and the v0.1.0 tagger field\" so the surface is recorded. If the decision is ever revisited, the tag must be re-created alongside the rewrite; a filter over commits alone will leave it.", "batch": "publish-the-repo"}
{"id": "fnd-105", "kind": "finding", "title": "docs/data/README.md names krimsonkla/recension, a repository that does not resolve", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "personal-data", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["docs/data/README.md:8 reads \"The format follows the one in `krimsonkla/recension`.\" `gh repo view krimsonkla/recension` -> \"Could not resolve to a Repository\". `gh api repos/krimsonkla/devenv-layers --jq .visibility` -> private, so findings.jsonl's fnd-004 quote is still accurate. The finder is also right that @krimsonkla is a deliberate public identifier (CODE_OF_CONDUCT.md:22, pyproject.toml:27-29, src/refugia/__init__.py:10 USER_AGENT) and not a leak. Worth adding: devenv-layers no longer appears in devenv.yaml or devenv.nix — I checked, devenv.yaml now declares only `github:cachix/git-hooks.nix` — so the only surviving mention is prose in the ledger, which reinforces that the real remedy here is trimming docs/data/ out of the published surface rather than editing it line by line.", "paths: docs/data/README.md:8"], "updated": "2026-09-07", "notes": "Describe the envelope inline (it is already fully documented in §1 of the same file) and drop the cross-reference, or reword to \"adapted from a private sibling project\". Leave fnd-004's devenv-layers quote alone — it is load-bearing evidence for a closed finding.", "batch": "prose-guard"}
{"id": "fnd-106", "kind": "finding", "title": "docs/guide/using-the-page.md claims \"no server\" and never says the profile name and home county travel in the file — both disclosures live only in the README and the CLI", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "personal-data", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["New surface from the last 25 commits (the guide split), so the first audit could not have seen it, and the finder did not look at it. Two gaps, both about what a shared page discloses about its author.\n\n(1) using-the-page.md:3 — \"writes one self-contained HTML file. Everything below happens in your browser, with no server\". The page is not contactless: template.html:2-6 carries `<link rel=preconnect href=https://fonts.googleapis.com>` and a Google Fonts stylesheet, so every viewer's browser reaches a third party on load. README.md:294-298 discloses this honestly and in detail (\"every viewer's browser contacts fonts.googleapis.com\"); the guide page that now covers the same ground for users does not, and its wording actively implies otherwise.\n\n(2) builder.py:58 is `payload[\"profile\"] = profile.to_dict()`, and profile.py:51-58 serialises `name` and `home_fips` — so a published page embeds the author's free-text profile name (the deleted personal one was \"Jason — allergens first\") and their home county. cli.py:117 says so at publish time (\"the profile behind it, including your home county {home_fips}\") and tests/test_sharing_note.py pins it — that part is genuinely well handled. But using-the-page.md's \"Hand the file to somebody\" paragraph mentions only weights, and writing-a-profile.md:62-67 documents home_fips and name with no note that they travel. Anyone who reads the guide instead of running publish never sees the warning.", "paths: docs/guide/using-the-page.md:3-9, docs/guide/writing-a-profile.md:62-67, src/refugia/artifact/builder.py:58, src/refugia/cli.py:117", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "In docs/guide/using-the-page.md, soften line 3 to name the one exception and point at README.md's font paragraph, and extend the \"Hand the file to somebody\" paragraph to say the page carries the profile's `name` and `home_fips` as well as its weights. In docs/guide/writing-a-profile.md, add one clause to the `home_fips` and `name` entries: both are embedded in any page you publish. No code change needed — cli.py:117 already does the right thing. FIXED: using-the-page says the profile travels with the page and that the typefaces come from Google.", "batch": ""}
{"id": "fnd-107", "kind": "finding", "title": "A duplicate spec key raises as claimed, but as a bare traceback that names no file, and no test covers it", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "reproducibility", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced: copied the shipped `wildfire_risk.json` into `<root>/specs/` and ran `refugia metrics` -- a full Rich traceback ending `ValueError: metric key 'wildfire_risk' already supplied by DeclarativeSource`. registry.py:25-28 interpolates only `type(source).__name__`, so when both sides are declarative specs the message identifies neither file. The second half also holds: tests/test_specs.py:57-63 has the docstring \"Duplicate keys raise at registration, which is the composition root's job\" over a body that only asserts `len(keys) == len(set(keys))` across the shipped set -- it never constructs a duplicate. And docs/guide/adding-a-spec.md:128-131 does invite `<root>/specs`, while step 2 promises \"the registry rejected it and will say why\", which a stack trace only half delivers.", "paths: src/refugia/metrics/registry.py:24-28, src/refugia/workspace.py:84-85, tests/test_specs.py:57-63, docs/guide/adding-a-spec.md:128-131", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "In `Workspace.build_registry`, wrap the spec loop: `except ValueError as error: raise ValueError(f\"{spec}: {error}\") from error` -- that puts the offending path in the message without touching the registry's contract. Optionally catch it in cli.py and `typer.Exit(1)` so the user sees a line instead of a traceback. Then add a test that writes a duplicate of a shipped key into a tmp `specs/` and asserts the raise names the file, and fix the misleading docstring on tests/test_specs.py:58. FIXED: Workspace re-raises a spec's ValueError prefixed with the file, so a duplicate key names the file you can act on.", "batch": ""}
{"id": "fnd-108", "kind": "finding", "title": "CI's only cross-implementation check depends on node being preinstalled in the runner image, which ci.yml never declares", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "reproducibility", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["ci.yml has no `actions/setup-node` and no node version anywhere; the conformance test (tests/test_scoring_conformance.py:119-126) resolves node via `shutil.which`. I went further than the finder and read the actual CI log for run 34140549497 (the latest push to main): the Test step reports `201 passed`, i.e. zero skips, so ubuntu-latest does carry a node today and the check is genuinely running -- the exposure is image drift, not a check that is silently off. The fail-in-CI branch is armed because GitHub sets `CI=true` for free.", "paths: .github/workflows/ci.yml:19-30, tests/test_scoring_conformance.py:119-126", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Insert before the Test step: `- uses: actions/setup-node@v4` with `node-version: '22'` (or 24, to match devenv's nodejs-24.19.0 -- pinning them to the same major is the point). Two lines, and the runner-image dependency becomes declared. FIXED: actions/setup-node pins node 22 rather than relying on the runner image.", "batch": ""}
{"id": "fnd-109", "kind": "finding", "title": "CONTRIBUTING tells a new contributor they will see a dangling .pre-commit-config.yaml in a fresh clone; they will not", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "reproducibility", "cycle": "cyc-release-audit-2", "finding_kind": "doc-contradicts-code", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["CONTRIBUTING.md:33 reads \"You will see a broken `.pre-commit-config.yaml` symlink in a fresh clone.\" The file is gitignored (.gitignore:7) and untracked, so it is not in a clone at all -- I listed my fresh clone and there is no such entry, while the maintainer's working tree has it as `lrwxr-xr-x ... -> /nix/store/...-pre-commit-config.json`. It appears only after a `devenv shell` entry generates it. A first-time contributor following this paragraph looks for a file that is not there and concludes their clone is wrong.", "paths: CONTRIBUTING.md:33-36, .gitignore:7", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Reword to: 'After a `devenv shell`, a `.pre-commit-config.yaml` symlink into the nix store appears in your tree. It is generated and gitignored; without devenv it dangles. Ignore it.' FIXED: Same fix as fnd-085.", "batch": ""}
{"id": "fnd-110", "kind": "finding", "title": "CONTRIBUTING.md and CLAUDE.md both tell contributors pytest runs with coverage by default; pyproject deliberately removed it", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "reproducibility", "cycle": "cyc-release-audit-2", "finding_kind": "doc-contradicts-code", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["pyproject.toml:44-47 carries an explicit comment: \"Coverage is not in addopts: with it there, running one file reports a project-wide total...\". But CONTRIBUTING.md:48-51 still says \"`pytest` is configured with coverage on by default, which makes a single-file run report a misleading project-wide total: `uv run pytest tests/test_engine.py` prints 20% against the whole suite's 59%\". I ran exactly that command in the fresh clone: `9 passed in 0.01s`, no coverage output at all. CLAUDE.md's Build & Test block likewise annotates `devenv shell -- pytest` as \"suite with coverage\", which it is not. The stale 59% figure is also now 60.92%. Documentation that describes a hazard the code was fixed to remove sends a contributor looking for a problem that no longer exists.", "paths: CONTRIBUTING.md:48-51, CLAUDE.md (Build & Test block), pyproject.toml:42-47", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Delete the CONTRIBUTING.md:48-51 paragraph or rewrite it as 'coverage is deliberately not in addopts; CI passes --cov explicitly so the number is produced once over the whole suite', and change CLAUDE.md's `devenv shell -- pytest` comment from 'suite with coverage' to 'suite'. Note tests/test_guide.py checks paths and links, not assertions like this one, so nothing catches it. FIXED: The coverage paragraph describes the current configuration: coverage is not in addopts.", "batch": ""}
{"id": "fnd-111", "kind": "finding", "title": "Nothing keeps the wheel carrying specs/ and template.html -- fnd-039's fix was verified once, by hand", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "reproducibility", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Independently rebuilt: `uv build` produces a wheel containing all six `refugia/specs/*.json` and `refugia/artifact/template.html` (113,939 bytes), and from a clean 3.12 venv in an unrelated directory the installed `refugia metrics` prints \"21 metrics\" and `refugia publish` writes a page -- so the fix is real today. The gap is also real: ci.yml (35 lines, read in full) runs only sync/ruff/black/pylint/pytest; it never builds, installs or executes the console script. One extra piece of evidence the finder missed strengthens this: pyproject.toml:44 sets `pythonpath = [\"src\"]`, so the whole suite -- including tests/test_specs.py:73 and tests/test_cli_metrics.py:20, the two cited as the guard -- imports from the source tree by construction and can never observe a packaging regression.", "paths: .github/workflows/ci.yml:47, pyproject.toml:38-45, tests/test_specs.py:73, tests/test_cli_metrics.py:20"], "updated": "2026-09-07", "notes": "Add a step after Test: `uv build`, `uv venv /tmp/wheelcheck`, `uv pip install --python /tmp/wheelcheck/bin/python dist/*.whl`, then `cd \"$(mktemp -d)\" && /tmp/wheelcheck/bin/refugia metrics | grep -q '21 metrics'`. Roughly ten lines and the only check that can see the packaged artifact rather than the source tree.", "batch": "packaging"}
{"id": "fnd-112", "kind": "finding", "title": "README promises 'Python 3.12 or newer' but CI only ever exercises 3.12, with warnings-as-errors on", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "reproducibility", "cycle": "cyc-release-audit-2", "finding_kind": "coverage-gap", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["ci.yml:24 is a flat `uv python install 3.12` with no matrix, while pyproject.toml:4 declares `requires-python = \">=3.12\"` and README.md:82 tells a stranger 'Python 3.12 or newer'. pyproject.toml:48 sets `filterwarnings = [\"error\"]`, so a DeprecationWarning emitted only under a newer interpreter -- exactly the kind httpx/openpyxl/typer produce on a new release -- turns into a red suite for a user on 3.13 or 3.14 that no CI run can see. I confirmed 3.14 passes today (200 passed, 1 skipped), so this is a durability gap rather than a live break.", "paths: .github/workflows/ci.yml:23-24, pyproject.toml:4, README.md:82"], "updated": "2026-09-07", "notes": "Give the check job `strategy.matrix.python: ['3.12', '3.14']` and use `uv python install ${{ matrix.python }}` / `uv run --python ${{ matrix.python }} pytest`. Keep the lint/format/pylint steps on 3.12 only if you want to hold CI time down; the point is that the suite runs on the newest interpreter the README promises.", "batch": "packaging"}
{"id": "fnd-113", "kind": "finding", "title": "The package is PyPI-shaped but the install-from-wheel path has no example profile and a README full of relative links", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "reproducibility", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Every fact checks out: the wheel's 53 entries contain no `profiles/example.json` (only `refugia/**`), the METADATA declares `Description-Content-Type: text/markdown` with the README verbatim, and README.md:22 and :31 embed `docs/screenshot-map.jpg` / `docs/screenshot-compare.jpg` while lines 41, 103, 134, 141, 151, 183, 185 link relatively into `docs/guide/` -- all of which PyPI would fail to resolve. What is overstated is the title's \"broken\": nothing is published, nothing points at PyPI, and README.md:82-89 documents clone + `uv sync`, which I ran end to end successfully. This is a precondition for a future `uv publish`, not a defect in the current shipping path, so it should be recorded as such rather than as a broken install.", "paths: pyproject.toml:1-27, README.md:22,31,41,103,134,141,151,183,185"], "updated": "2026-09-07", "notes": "No action needed to go public on GitHub. Add a release checklist item (CONTRIBUTING.md or CHANGELOG.md): before any `uv publish`, rewrite the README's image and guide links to absolute `https://github.com/krimsonkla/refugia/blob/main/...` URLs, and ship the example profile inside the package as `src/refugia/profiles/example.json` (resolved the way `Workspace.packaged_spec_dir` resolves specs) so an installed copy has something for `--profile` to point at.", "batch": "packaging"}
{"id": "fnd-114", "kind": "finding", "title": "`refugia metrics` creates a data/cache directory in whatever directory you run it in", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "reproducibility", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced twice, from source and from the installed wheel. `cd <empty tmp dir> && refugia metrics` leaves `<dir>/data/cache/` behind; cause is store/cache.py:26 `self._root.mkdir(parents=True, exist_ok=True)` inside `Cache.__init__`, called unconditionally from workspace.py:29 which cli.py:210 invokes for `metrics`. cli.py:208 does document it as side-effect-free (\"Reads no dataset, so it works before a fetch\"). ONE PIECE OF THE FINDER'S EVIDENCE IS WRONG and should not be carried into the fix: I ran `refugia metrics --root <rootdir>` from an empty cwd and the cwd stayed empty while `<rootdir>/data/cache/` was created; same for `publish --root <repo>` (it wrote `<cwd>/out/refugia.html` because `--out` defaults to a cwd-relative path, not `data/cache/`). So the Workspace is NOT built before `--root` is consulted, and there is no double cache. The litter happens only when `--root` is omitted, i.e. the default path.", "paths: src/refugia/store/cache.py:24-26, src/refugia/workspace.py:29, src/refugia/cli.py:205-210", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Drop the mkdir from `Cache.__init__` and create the directory at the point of writing: in `Cache.write` (before `target.write_bytes`) and in `ArtifactBuilder`'s cache use. Two call sites then need a missing-directory guard: `Cache.oldest_entry` (cache.py:67 `self._root.iterdir()` would raise FileNotFoundError -- return \"\" when the root does not exist) and `Cache.has`/`path_for`, which are already safe. A test asserting `refugia metrics` leaves an empty tmp cwd untouched is three lines and stops it recurring. FIXED: Cache creates its directory on first write rather than on construction, and oldest_entry returns '' for a directory that does not exist. Verified: running metrics from an empty directory leaves it empty. Two tests.", "batch": ""}
{"id": "fnd-115", "kind": "finding", "title": "black has no pinned target-version, so it warns on every run and skips its own AST safety check", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "reproducibility", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced locally (black 26.5.1 under CPython 3.12.14) and, which the finder did not check, in CI: run 34140549497's Format step prints the same line -- \"Warning: Python 3.12 cannot parse code formatted for Python 3.15...\" -- before \"65 files would be left unchanged\", exit 0. `[tool.black]` at pyproject.toml:90-91 sets only `line-length` while `[tool.ruff]` at :66 does set `target-version = \"py312\"`. One wording correction: black does not \"skip\" the safety check on purpose; it reports that the equivalence check cannot parse a target newer than the running interpreter. The remedy is unchanged.", "paths: pyproject.toml:90-91", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Add `target-version = [\"py312\"]` under `[tool.black]`, matching `[tool.ruff]`. It silences the warning on every contributor's run and in every green CI log, and pins the formatting target to the same thing twice. FIXED: black target-version pinned to py312, so it no longer warns and no longer skips its own equivalence check.", "batch": ""}
{"id": "fnd-116", "kind": "finding", "title": "ArtifactBuilder's docstring still says nothing executes both scorers", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "security-quality", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["builder.py:37-41 reads '…nothing in this repo currently executes both to compare them.' tests/test_scoring_conformance.py:105-128 does exactly that through tests/conformance/run_vectors.mjs, and CLAUDE.md:83-88 already states the corrected version. Violates the repo's own 'a comment describes the thing as it is' rule, and it is the load-bearing safety claim.", "paths: /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/artifact/builder.py", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Replace builder.py:38-41 with the CLAUDE.md:83-88 wording: the duplication is forced, tests/conformance/vectors.json is run by both implementations, and the JavaScript half must stay between the `scoring kernel: BEGIN/END` markers and stay pure so it can be lifted out. FIXED: The docstring says the conformance vectors exist and names what they do not cover.", "batch": ""}
{"id": "fnd-117", "kind": "finding", "title": "CI declares no `permissions:` block and pins every action to a floating major tag", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "security-quality", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": [".github/workflows/ci.yml has `on: push/pull_request` and a single `check` job with no `permissions:` key, so GITHUB_TOKEN gets whatever the repository default is — write-all on older repository settings. Nothing in the job needs any token scope beyond reading the checkout. Both actions (actions/checkout@v7 at :20, astral-sh/setup-uv@v7 at :22) are pinned by mutable major tag rather than commit SHA; dependabot.yml covers version drift but not tag repointing. Neither is exploitable today on a private repo, and the `pull_request` (not `pull_request_target`) trigger already means fork PRs get a read-only token — this is the standard hardening a public repo picks up.", "paths: /Users/krimsonkla/git/krimsonkla/refugia/.github/workflows/ci.yml", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Add `permissions: contents: read` at the workflow level in .github/workflows/ci.yml. If you want the supply-chain half too, pin both actions to full commit SHAs with the tag in a trailing comment; dependabot updates SHA pins as happily as tags. FIXED: Same fix as fnd-088; actions/setup-node added so the conformance check does not depend on the image.", "batch": ""}
{"id": "fnd-118", "kind": "finding", "title": "Retry treats every httpx.TransportError and every OSError as transient", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "security-quality", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["retry.py:57 is `return isinstance(error, (httpx.TransportError, OSError))`, and I confirmed in the venv that httpx.UnsupportedProtocol, httpx.LocalProtocolError and httpx.ProxyError are all TransportError subclasses, so all are retried four times over 10.5s of backoff. tests/test_retry.py covers status codes and a generic non-httpx error only. The exposure is genuinely theoretical today — every URL is either an https constant or validated by declarative.py:73-75 — so this is about the row's evidence overstating how narrow the classification became, plus the future hazard the finder names (a callable doing file I/O would get FileNotFoundError retried).", "paths: /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/retry.py, /Users/krimsonkla/git/krimsonkla/refugia/tests/test_retry.py", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "In Retry.is_transient, return False early for (httpx.UnsupportedProtocol, httpx.LocalProtocolError), and replace the bare OSError with (ConnectionError, TimeoutError). Add the two parametrised cases to tests/test_retry.py next to test_a_permanent_answer_is_not_retried. FIXED: Retry raises UnsupportedProtocol and LocalProtocolError at once, and narrows bare OSError to ConnectionError and TimeoutError. One test.", "batch": ""}
{"id": "fnd-119", "kind": "finding", "title": "SECURITY.md's `ask` trust boundary describes only the CLI path and does not mention the published page's Ask panel", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "security-quality", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["SECURITY.md's fourth boundary reads '**`ask` talks to a model.** By default a local Ollama on your own machine, which is where the question and the rows stay. `--host` will point it somewhere else…' — that is cli.py:273-288 / ask/planner.py. It says nothing about the other ask, the one in the shared artifact: template.html:3089-3100 requests the host's `sample` capability and template.html:3031-3078 builds a context header carrying the viewer's weights, requirements, counts and home-county fit, which then leaves the browser to whatever model the host provides. README.md:190-200 does describe this honestly; SECURITY.md, the document that tells a reporter where the boundaries are, describes the opposite property for a path with the same name. This is also the path a shared page exposes to someone who did not build it.", "paths: /Users/krimsonkla/git/krimsonkla/refugia/SECURITY.md, /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/artifact/template.html", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Add a sentence to SECURITY.md's `ask` paragraph distinguishing the two front ends: the CLI talks to a local Ollama unless --host says otherwise; the published page's Ask panel appears only where a host offers window.claude.use and then sends the view context to that host's model on the viewer's own account. Point at README.md#asking-questions for the detail. FIXED: SECURITY.md's ask boundary now covers the page's Ask panel as well as the CLI.", "batch": ""}
{"id": "fnd-120", "kind": "finding", "title": "The rate floor exists only on LANDFIRE — but the policy is documented in three places, contrary to the finder's remedy", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "security-quality", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["The code half is right: `grep -rn Throttle src/` returns only throttle.py and landfire_vegetation.py:77, while nasa_power_climate.py:125 opens a 6-worker pool over PARAMETERS x tiles with no floor, and city_profile.py:72's 8-worker pool correctly inherits LANDFIRE's throttle through the injected source. The framing is wrong. The finder's remedy is 'state in CLAUDE.md that the floor is per-adapter … and add it to the checklist in docs/guide/adding-an-adapter.md' — all of that already exists: CLAUDE.md:89-92 ('a source that fans out over a worker pool takes a Throttle'), CONTRIBUTING.md:115-117, and docs/guide/adding-an-adapter.md:72 ('If you fan out over a worker pool, take a Throttle'). So the real finding is narrower and sharper than stated: NasaPowerClimateSource violates the project's own written rule.", "paths: /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/metrics/sources/nasa_power_climate.py, /Users/krimsonkla/git/krimsonkla/refugia/tests/test_outbound_requests.py", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Give NasaPowerClimateSource a Throttle (a min_interval constructor argument, waited on in _request_once before the httpx.get at nasa_power_climate.py:203), so the documented rule and the code agree. Optionally extend the AST sweep in tests/test_outbound_requests.py to fail a module that constructs a ThreadPoolExecutor around an httpx call without also constructing a Throttle — the same structural shape already used for USER_AGENT. No documentation change is needed. FIXED: Same fix as fnd-097: the rate floor now exists on both fan-out sources.", "batch": ""}
{"id": "fnd-121", "kind": "finding", "title": "The structural User-Agent guard only sees `httpx.get`/`httpx.post`, so the sixth adapter it exists for can evade it", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "security-quality", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["tests/test_outbound_requests.py:24-27: `if target.attr in (\"get\", \"post\") and getattr(target.value, \"id\", None) == \"httpx\"`. A contributed adapter written as `client = httpx.Client(); client.get(url)` — or using httpx.request / httpx.stream, both of which tests/conftest.py:33-35 bothers to block for the offline rule — yields no call sites at all and the parametrised test simply does not run for that module. The canary at :48-50 only asserts >=5 sites exist overall, so it stays green. The docs state the rule as enforced ('will fail your PR if a request identifies nobody'), which is exactly the kind of claim a public repo should not overstate. The finder cited this test approvingly as evidence fnd-016 is done.", "paths: /Users/krimsonkla/git/krimsonkla/refugia/tests/test_outbound_requests.py, /Users/krimsonkla/git/krimsonkla/refugia/CLAUDE.md, /Users/krimsonkla/git/krimsonkla/refugia/CONTRIBUTING.md", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Widen _request_calls to any attribute call in ('get','post','put','request','stream','send') whose receiver is `httpx` or a name bound to an httpx.Client, and additionally fail any module that constructs httpx.Client without headers containing USER_AGENT. Mutation-check it the way the original was: add a Client-based call and confirm the test goes red. FIXED: Same fix as fnd-076.", "batch": ""}
{"id": "fnd-122", "kind": "finding", "title": "Two `while True` paging loops have no page cap and do not check the page's type", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "security-quality", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["cdc_places.py:130-137 and city_profile.py:135-141 are exactly as quoted; the only exit is a short page. The dict-envelope variant is also real: rows.extend(a dict) extends with its keys, len() then terminates the loop, and cdc_places.py:139-142 / city_profile.py:106-107 call .get on a str. Likelihood is low — Socrata answers a bad query with a non-200 that response.raise_for_status() catches first — so this is a hardening item, not a live bug. The finder's aside is correct that in cdc_places the blast radius is _collect's per-source catch (five metrics), while on the publish path nothing catches it (see missed-2).", "paths: /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/metrics/sources/cdc_places.py, /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/artifact/city_profile.py", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Add a MAX_PAGES constant to each loop and raise a named error on overrun, and assert isinstance(page, list) immediately after the _retry.run call in both so a malformed envelope fails at the boundary with a readable message. FIXED: Both paging loops are bounded by MAX_PAGES and check the page is a list before extending, so a 200 carrying an object raises instead of extending rows with its keys. One test.", "batch": ""}
{"id": "fnd-123", "kind": "finding", "title": "`publish` makes live network calls with no failure handling, so the traceback fnd-020 removed from `fetch` still exists on the other command", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "security-quality", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["cli.py:267 is a bare `written = ArtifactBuilder(workspace.cache).build(dataset, loaded, out)`. That call reaches CountyShapes (geometry.py:39 fetch_url of the us-atlas topology), CityMarkers (city_markers.py:137,142 two Census downloads) and CityProfile (city_profile.py:120-134 the Socrata place release plus a 8-worker LANDFIRE resample). On a cold cache with any of those upstreams moved or down, `refugia publish` ends in an httpx traceback — the precise user-facing failure fnd-020 ('one dead upstream costs one metric, not the whole fetch', and 'a missing dataset prints a traceback') set out to remove, fixed on `fetch` only. The city-profile layer is already optional by design (a city falls back to its county), so a failure there should be a warning, not the end of the command.", "paths: /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/cli.py, /Users/krimsonkla/git/krimsonkla/refugia/src/refugia/artifact/builder.py"], "updated": "2026-09-07", "notes": "Wrap the optional enrichment inside ArtifactBuilder.build — city markers and city metrics — in a per-step try/except that degrades to an empty payload key and returns a reason, and have cli.py:publish print it the way _report_failures does. Leave the county topology fatal if you like, but catch httpx.HTTPError/OSError in cli.py:publish and exit with a sentence instead of a traceback.", "batch": "upstream-resilience"}
{"id": "fnd-124", "kind": "finding", "title": "An emptied vectors.json turns the whole conformance suite green", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced exactly. Writing `{\"cases\": []}` to tests/conformance/vectors.json in a scratchpad copy gives `2 passed, 1 skipped` for tests/test_scoring_conformance.py: the parametrised test skips on an empty argvalues, `test_the_ordering_matches_the_vector` (line 80) loops over nothing, and run_vectors.mjs prints \"all 0 vectors match\" and exits 0 so the node assertion at line 122 holds. Nothing asserts CASES is non-empty. The finder is also right that this is out of step with the repo's own convention — tests/test_specs.py:24, tests/test_outbound_requests.py:48 and tests/test_guide.py:35 each have exactly the guard this file lacks.", "paths: tests/test_scoring_conformance.py, tests/conformance/run_vectors.mjs"], "updated": "2026-09-07", "notes": "Add `def test_there_are_vectors_to_check(): assert len(CASES) >= 12; assert all(c[\"expect\"] for c in CASES)` to tests/test_scoring_conformance.py next to CASES (line 30), and the same length floor in run_vectors.mjs before the `for (const c of cases)` loop, exiting 2 if it fails — the runner is invoked as a subprocess, so a Python-only guard leaves the node half able to pass vacuously if it is ever run directly.", "batch": "scoring-parity"}
{"id": "fnd-125", "kind": "finding", "title": "ArtifactBuilder's docstring still says nothing executes both scorers", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Verbatim at src/refugia/artifact/builder.py:36-41: \"That duplication is a real hazard: the JavaScript and the Python engine can drift, and nothing in this repo currently executes both to compare them.\" tests/test_scoring_conformance.py:95 `test_the_page_kernel_matches_the_same_vectors` does exactly that, and I confirmed it bites — mutating `filter(([, w]) => w > 0)` to `>= 0` at template.html:1270 fails it. CLAUDE.md: \"A comment describes the thing as it is, never how it got there.\" This is a docs defect inside a source file rather than a test defect, but the misdirection is about the test suite so it belongs in this lens.", "paths: src/refugia/artifact/builder.py", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Rewrite builder.py:36-41 to say the duplication is held in agreement by tests/conformance/vectors.json, run against both engines by tests/test_scoring_conformance.py, and that the JS half is lifted from between the `scoring kernel: BEGIN`/`END` markers in template.html — so a change to ranking semantics must move the markers with the code. Say plainly what the vectors do not cover (normalization and criteria) until finding #1 is closed. FIXED: Same fix as fnd-116.", "batch": ""}
{"id": "fnd-126", "kind": "finding", "title": "The autouse no-network fixture does not cover httpx.AsyncClient or a raw transport; my probe reached DNS", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["tests/conftest.py:38-40 patches `httpx.get/post/request/stream` and `httpx.Client.send`. I wrote four probe tests into a scratchpad copy of tests/. Two raised the fixture's AssertionError as intended. The other two — `asyncio.run` over `httpx.AsyncClient().get(...)`, and `httpx.HTTPTransport().handle_request(...)` — escaped the fixture and failed with `httpx.ConnectError: [Errno 8] nodename nor servname provided, or not known`, i.e. they actually attempted a name lookup. The docstring says \"Make a real request impossible, so the suite cannot quietly grow one\", and the failure mode it names — \"a new adapter test that passes on the author's machine and hangs in CI\" — is exactly what an async adapter would still do. Nothing in src/ is async today, so this is latent. The finder noted the AsyncClient gap inside its fnd-053 verification but did not raise it as an actionable item.", "paths: tests/conftest.py"], "updated": "2026-09-07", "notes": "Add `monkeypatch.setattr(httpx.AsyncClient, \"send\", _refuse)` beside the existing `httpx.Client.send` patch at tests/conftest.py:40. Patching `httpx.HTTPTransport.handle_request` and `httpx.AsyncHTTPTransport.handle_async_request` as well closes the transport-level route and makes the docstring's claim literally true; that is two more lines.", "batch": "tests-that-bite"}
{"id": "fnd-127", "kind": "finding", "title": "The direction inversion is re-derived outside the kernel markers for the map colour ramp", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Verified. Markers at template.html:1215 (BEGIN) and :1310 (END); `percentile` :1225, `normalizeAll` :1249, `scoreAll` :1268 are all inside, and :1353 is the only `scoreAll` call site, so run_vectors.mjs does lift what the page runs. template.html:1911-1913 is a character-for-character copy of the :1255-1259 expression, outside the markers. Two corrections to scope: (a) there are further `direction` reads outside the markers at :1324 (home cuts), :1411 (cut label), :1917/:1921 (legend text) and :2462 (city comparison) that the finder did not list — those are comparison/labelling rather than duplicated normalisation, so CLAUDE.md's \"applied in exactly one place\" is stretched more widely than the report suggests; (b) a divergence here repaints the map, it does not change any ranking, so nice-to-have is the right gate.", "paths: src/refugia/artifact/template.html"], "updated": "2026-09-07", "notes": "Extract `function directed(m, obs)` into the kernel between the BEGIN/END markers (it is the body of the loop at :1255-1259), have `normalizeAll` call it, and replace :1911-1913 with `vals = directed(m, obs)`. Export it from the marker block so run_vectors.mjs's `return { percentile, normalizeAll, scoreAll }` can pick it up if you later want a vector over it. Leave :1324/:1411/:1917/:2462 alone — they read direction to phrase a comparison, which is a different thing.", "batch": "scoring-parity"}
{"id": "fnd-128", "kind": "finding", "title": "Three scoring mutants survive the full suite: the minmax flat-field constant, the NaN filter, and the min_coverage default", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["I ran all three mutants myself against the full suite in a scratchpad copy, and all three survived at `201 passed`: (1) normalizer.py:49 `dict.fromkeys(values, 50.0)` -> `0.0`; (2) normalizer.py:31 `if v is not None and not math.isnan(v)` -> `if v is not None`; (3) profile.py:22 `min_coverage: float = 0.8` -> `0.0`. Coverage independently agrees — normalizer.py 89%, missing lines 25 and 49. I also spot-checked the finder's counter-claim that the engine is well covered and agree: engine.py:47 `s.coverage >= profile.min_coverage` -> `>` kills 2 tests, and `reverse=True` -> `False` kills 3. (One engine mutant does survive, `sum(abs(w) ...)` -> `sum(w ...)` at engine.py:107, but that is an equivalent mutant — `normalized_weights` already filters `w > 0` — so it is dead defensive code, not a test gap.)", "paths: src/refugia/scoring/normalizer.py, src/refugia/scoring/profile.py, tests/test_normalizer.py, tests/test_profile.py"], "updated": "2026-09-07", "notes": "Three assertions, all trivial: in tests/test_normalizer.py, `Normalizer(method=\"minmax\").normalize(parks, {\"a\":5,\"b\":5}) == {\"a\":50.0,\"b\":50.0}`; a NaN value is absent from the returned dict; in tests/test_profile.py, `Profile(\"p\").min_coverage == 0.8`. Note the `minmax` hole compounds finding #1 — minmax is a documented user option that neither the vectors nor the page exercise.", "batch": "scoring-parity"}
{"id": "fnd-129", "kind": "finding", "title": "Throttle's lock is not load-bearing in any test; the whole 201-test suite passes with it removed", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "tests", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced: replacing `with self._lock:` at src/refugia/metrics/sources/throttle.py:38 with `if True:` gives `201 passed in 0.31s`. All five tests in tests/test_throttle.py (lines 21, 28, 37, 47, 56) drive a single-threaded `_Clock`, so the concurrency property is untested by construction. Wiring confirmed at landfire_vegetation.py:77 `self._throttle = Throttle(...)`, :142 `ThreadPoolExecutor(max_workers=self._sampling.workers)`, :233 `self._throttle.wait()` — and coverage puts landfire_vegetation at 30% with 224-262 unexecuted, so the integration is untested too. Note the class is reported at 100% line coverage, which is precisely the misleading part.", "paths: src/refugia/metrics/sources/throttle.py, tests/test_throttle.py"], "updated": "2026-09-07", "notes": "Add to tests/test_throttle.py one test that runs N threads through a single shared Throttle against a monotonic fake clock and asserts consecutive recorded start times are >= min_interval apart — that mutant then dies. The landfire `_sample` integration test the finder also asks for is a separate, larger job (it needs a recorded payload) and belongs under fnd-065, not here.", "batch": "tests-that-bite"}
{"id": "fnd-130", "kind": "finding", "title": "test_every_shipped_spec_loads asserts a condition that cannot fail, and nothing checks the specs survive a packaging change", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["tests/test_specs.py:34 reads `assert source.metrics[0].key == spec.stem or source.metrics[0].key` — the right operand is a non-empty string for every shipped spec, so the assertion is unfalsifiable except on an empty key, and line 39 makes the stem check properly anyway. The packaging half also checks out: I ran `uv build` and `unzip -l` on the wheel myself and all six `refugia/specs/*.json` plus `refugia/artifact/template.html` are present (113939 bytes), so the wheel is correct today; and tests/test_specs.py:72 does run against `src/` via pytest's `pythonpath = [\"src\"]` (pyproject.toml:52), not against an install, so a hatch `exclude` would break shipping with the suite green.", "paths: tests/test_specs.py, pyproject.toml"], "updated": "2026-09-07", "notes": "Replace line 34 with `assert source.metrics` (the value of the test is that construction runs the validator without raising). For the packaging half, add a test that resolves the spec directory via `importlib.resources.files(\"refugia\") / \"specs\"` and asserts six `.json` files plus `importlib.resources.files(\"refugia.artifact\") / \"template.html\"` exists — under an editable install that still passes today and it turns red the moment the data files stop being declared.", "batch": "tests-that-bite"}
{"id": "fnd-131", "kind": "finding", "title": "test_guide never parses DATA_SOURCES.md, CONTRIBUTING.md or CLAUDE.md, and exempts README.md from the metric-key check", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Confirmed by reading: tests/test_guide.py:27 `DOCS = [*PAGES, ROOT / \"README.md\"]`, and `test_every_metric_key_the_guide_names_is_registered` (line 63) iterates `PAGES`, not `DOCS`. The docstring's own motivating example is a DATA_SOURCES paragraph, and DATA_SOURCES.md is not in DOCS. I ran the checker's own two regexes over every root markdown file myself: every path named in DATA_SOURCES.md, CONTRIBUTING.md, CLAUDE.md, SECURITY.md, CHANGELOG.md and CODE_OF_CONDUCT.md currently exists, so this is latent, not live — the finder said so and was right.", "paths: tests/test_guide.py"], "updated": "2026-09-07", "notes": "Set `DOCS = [*PAGES, ROOT/\"README.md\", ROOT/\"DATA_SOURCES.md\", ROOT/\"CONTRIBUTING.md\", ROOT/\"CLAUDE.md\"]` at line 27 and iterate `DOCS` in the key test at line 74. I ran the widened check: the only new tokens it flags are `terms_url` (DATA_SOURCES.md, CONTRIBUTING.md), `raise_for_status` (CLAUDE.md) and the six planner tool names in README.md — `compare_places`, `explain_subset`, `list_states`, `top_places`, `weighted_deficit`, plus `min_coverage` which is already allowlisted. So the one-off pass is six additions to the `field_names` set at line 76 and nothing else.", "batch": "prose-guard"}
{"id": "fnd-132", "kind": "finding", "title": "test_outbound_requests matches only `httpx.get`/`httpx.post` on the `httpx` name, so a Client-based or `httpx.request` adapter is invisible to it", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "tests", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Read confirms it: tests/test_outbound_requests.py:26-31 requires `target.attr in (\"get\", \"post\")` AND `getattr(target.value, \"id\", None) == \"httpx\"`. I reproduced the probe on a scratchpad copy — a module at src/refugia/metrics/sources/probe_adapter.py containing `with httpx.Client() as client: client.get(url)` and `httpx.request(\"GET\", url)`, both header-free — and got `6 passed`, nothing flagged. Severity corrected down: all five shipped adapters do carry the header (the `>= 5` guard at line 51 is real and passes), so there is no live defect and nothing leaks. The cost of the blind spot is entirely prospective — it is a review aid for an outside PR that does not exist yet.", "paths: tests/test_outbound_requests.py", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "In `_request_calls` (line 22), drop the `target.value.id == \"httpx\"` constraint and widen the verb set to `{get, post, put, patch, delete, head, options, request, stream, send}`, so any receiver is matched. Separately walk for `ast.Call` whose func resolves to `httpx.Client`/`httpx.AsyncClient` and assert a `headers=` keyword mentioning USER_AGENT. Keep the `>= 5` non-empty guard at line 51 and bump it if the widening finds more sites. FIXED: Same fix as fnd-076.", "batch": ""}
{"id": "fnd-133", "kind": "finding", "title": "\"Using the page\" describes the requirements panel as removing, but two of its controls do not", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["The omission is real: template.html:1529-1573 renders six controls — minimum population (:1531), maximum home value (:1533), rule-out states (:1535), the per-metric cut summary (:1544), an \"Only places better than <home>\" checkbox (:1549), a fit-threshold slider (:1555) and a coverage slider (:1562). docs/guide/using-the-page.md:15-18 names three plus the per-metric cut; the home-anchor checkbox, the fit slider and the coverage slider are undocumented, and reading-the-numbers.md:11-12 (\"Anything below `min_coverage` never appears at all\") does not say the reader can move it (template.html:1602 sets minCoverage live). Overstated in one respect: the finder frames the fit slider as behaving \"in the way the documented sentence explicitly denies\", but the page states the exception inline itself at template.html:1571-1573 (\"This one hides rather than removes…\"), so no page reader is actually misled — only a guide-only reader is incompletely informed. Down from important to nice-to-have on that basis.", "paths: docs/guide/using-the-page.md:15-18, docs/guide/reading-the-numbers.md:11-12, src/refugia/artifact/template.html:1544-1573"], "updated": "2026-09-07", "notes": "After using-the-page.md:18 add a short list of the rest of the panel: the \"Only places better than <home>\" checkbox (present only when the profile sets home_fips, template.html:1546-1552), the fit slider (hides rather than removes, because fit is a rank among the places that met your requirements), and the coverage slider (the live `min_coverage`, lowering it admits places with gaps). At reading-the-numbers.md:12 add \"…and the page lets you move that threshold from the requirements panel.\"", "batch": "prose-guard"}
{"id": "fnd-134", "kind": "finding", "title": "A page published from `--universe all` still says \"in a metro or micropolitan area\"", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["src/refugia/artifact/builder.py:85-95 `_subtitle` returns the literal \"{n:,} US counties in a metro or micropolitan area, scored on …\" and takes only (dataset, profile). src/refugia/store/dataset.py carries places/metrics/values/built_at/oldest_response — no universe field — so the string cannot be conditioned on it. command-line.md:41,:53-56 documents `all` as every US county. Severity corrected down: the sentence is only wrong under the non-default `--universe all` (a `metro` fetch is a subset of \"metro or micropolitan\", so that case stays true), nothing is broken and nothing private leaks. It is a cosmetic mislabel on a shared file, not an important defect.", "paths: src/refugia/artifact/builder.py:85-95, src/refugia/store/dataset.py:25-29, docs/guide/command-line.md:53-56"], "updated": "2026-09-07", "notes": "Simplest: change builder.py:93 to \"{n:,} US counties, scored on …\" — the count already tells the reader the size of the universe. Better: add `universe: str = \"\"` to Dataset (set in cli.fetch:181-187, defaulted on load at dataset.py:86 so old files still parse) and have _subtitle name it, then note in command-line.md:53-56 what the page's subtitle says for each universe.", "batch": "prose-guard"}
{"id": "fnd-135", "kind": "finding", "title": "README's \"declarative tier above 92%\" measures 88%", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "fix-overstated", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Ran `uv run pytest --cov=refugia --cov-report=term` (201 passed, 0.65s). README.md:264-265 claims \"The scoring core, the store and the declarative tier are all above 92%\". Measured: metrics/sources/declarative.py 88% (106 stmts, 8 missed, 38 branches, 5 partial). The store holds (cache.py and dataset.py both 100%). The scoring claim only holds as a package aggregate — per module, normalizer.py is 89% and criterion.py is exactly 92% (not \"above\"), so the sentence is wrong for two of its three subjects, slightly worse than the finder reported.", "paths: README.md:264-265", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Restate without a number that decays: \"The scoring core and the store are at or near full coverage and the declarative tier just under 90%; the adapters are the thin part.\" If you keep a figure, add a `--cov-fail-under` per-module floor in CI at the number the README quotes so the two cannot drift. FIXED: Measured and corrected: scoring core and store at or near 100%, declarative tier 87%.", "batch": ""}
{"id": "fnd-136", "kind": "finding", "title": "The \"rule out anywhere worse than home\" cut exists only on a weighted metric, and switching a metric off silently discards an active cut", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Three documents say every metric carries the cut — using-the-page.md:17 \"each metric additionally offers\", writing-a-profile.md:64 \"offers each metric's 'rule out anywhere worse than home' cut\", README.md:25 \"each metric carries its own cut\". template.html:1405-1411 returns an empty string unless home has a value for that metric AND `(weights[m.key] || 0) > 0`, so the control is absent on every unweighted metric — which, since the recent \"keep every column\" change, is now a visible row in the table with no cut on it. And at template.html:1516-1521, unchecking a metric sets `cuts[k] = false`: a hard requirement the user set silently disappears when they drop that metric's weight, and the ranking widens without a word. That is a real requirement vanishing, which is the one class of change the docs insist is loud.", "paths: docs/guide/using-the-page.md:15-18, docs/guide/writing-a-profile.md:62-65, README.md:24-26, src/refugia/artifact/template.html:1405-1419, src/refugia/artifact/template.html:1516-1521"], "updated": "2026-09-07", "notes": "In using-the-page.md, qualify the sentence: the cut appears on a metric that is switched on and for which home has a value, and switching that metric off clears the cut as well as the weight. Make the same qualification at writing-a-profile.md:64. Ideally also surface it in the page's cut summary (template.html:1544) so the user sees the requirement leave.", "batch": "prose-guard"}
{"id": "fnd-137", "kind": "finding", "title": "The `--explain` sample silently omits a row from the middle of real output", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Ran `uv run refugia rank --profile profiles/example.json --top 1 --explain` against the checked-in dataset. Live output is populus_cover +6.4, juniper_cover +6.3, sagebrush_cover +6.3, air_pollution +6.2, poor_mental_health_days +4.8, poor_or_fair_health +4.8 raw=9.8, life_expectancy +4.7. README.md:118-129 and command-line.md:19-29 jump straight from poor_mental_health_days to life_expectancy — the poor_or_fair_health row is deleted from the middle, and the trailing `...` only signals truncation at the tail. Every other figure matches exactly.", "paths: README.md:118-129, docs/guide/command-line.md:19-29"], "updated": "2026-09-07", "notes": "Paste `poor_or_fair_health    +  4.8   raw=9.8` back in after the poor_mental_health_days line in both blocks, or regenerate both from the command and cut only at the end.", "batch": "prose-guard"}
{"id": "fnd-138", "kind": "finding", "title": "The command reference opens \"Four commands\" — there are five", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["Reproduced. docs/guide/command-line.md:3 \"Four commands.\" and docs/guide/README.md:10 \"the four commands, every option and default\". src/refugia/cli.py declares five @app.command()s: fetch (:159), metrics (:195), rank (:225), publish (:253), ask (:270). The option table at command-line.md:39-51 does carry the `metrics` row, so only the prose miscounts. README.md:105-110 is the same four-line block. `refugia metrics` is the one command that works before a fetch and is missing from both quickstart blocks.", "paths: docs/guide/command-line.md:3, docs/guide/README.md:10, README.md:105-110", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Change \"Four commands\" to \"Five commands\" at command-line.md:3 and \"the four commands\" to \"the five commands\" at docs/guide/README.md:10, and add `uv run refugia metrics  # the vocabulary a profile is written against` as the first line of both code blocks (command-line.md:6-11, README.md:105-110). FIXED: 'Five commands', and the guide index agrees.", "batch": ""}
{"id": "fnd-139", "kind": "finding", "title": "The guide documents `ask`'s options but never that it needs a running Ollama and an 18 GB model", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["README.md:232-243 covers the prerequisite well, but the guide is presented as the place where \"full walkthroughs live\" (README.md:103) and command-line.md is where a reader goes for the commands. It shows `uv run refugia ask \"…\"` in its quickstart at :10 and lists --model/--host at :49-50 with no hint that anything outside `uv sync` is required, and the guide index has no ask page and no link back to the README's ask section. Mitigating, and why this is only nice-to-have: I ran `refugia ask` with no Ollama and the failure is self-explanatory — RuntimeError: could not reach Ollama at http://localhost:11434 … Start it with `ollama serve` and pull the model with `ollama pull qwen3:30b-a3b`.", "paths: docs/guide/command-line.md:10, docs/guide/command-line.md:49-50, docs/guide/README.md", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Add one line under the quickstart block in command-line.md: \"`ask` is the only command with a prerequisite outside `uv sync` — a local [Ollama](https://ollama.com) running and the model pulled (~18 GB); see the README's ask section. Everything else works without it.\" FIXED: The command-line page names the Ollama prerequisite and the model pull.", "batch": ""}
{"id": "fnd-140", "kind": "finding", "title": "The split left whole paragraphs duplicated between README and guide, and inside single pages", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["The verbatim duplications check out: README.md:112-114 is character-for-character command-line.md:13-15; command-line.md:3-4 repeats its own first clause at :13-14; the `--explain` block is the same in both; adding-an-adapter.md:121-122 (\"already gets a slider, a column and a map layer\") and :124-125 (\"arrives with a slider, a column and a map layer already\") are four lines apart. Overstated on one point: the county-grain sentence is verbatim in only two places (README.md:172-173 and using-the-page.md:41-43); reading-the-numbers.md:23-24 is a paraphrase, and some redundancy across an audience-split README/guide is deliberate. Also note the causal claim is inference, not evidence — fnd-025 and fnd-018 predate the split.", "paths: docs/guide/command-line.md:3-15, docs/guide/adding-an-adapter.md:121-125, README.md:105-131"], "updated": "2026-09-07", "notes": "Delete the repeated clause at command-line.md:13 (keep only \"Copy `profiles/example.json` and edit it — the weights are yours, and that is the point.\") and the second sentence at adding-an-adapter.md:124-125. Leave the README/command-line overlap alone or trim the README's copy to the code block plus a link; it is the one duplication that serves two audiences.", "batch": "prose-guard"}
{"id": "fnd-141", "kind": "finding", "title": "`--root` is documented as the parent of `out/`; publish ignores it", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["command-line.md:36-37 says --root \"is where `data/` and `out/` live\". src/refugia/cli.py:261 defaults out to Path(\"out/refugia.html\") and :267 passes it to ArtifactBuilder.build unchanged; builder.py:60 does out.parent.mkdir/out.write_text on that raw path. src/refugia/workspace.py derives only data/cache (:29), data/dataset.json (:39) and specs/ (:55) from the root — there is no `out` property, so the `out/` half of the sentence is simply false.", "paths: docs/guide/command-line.md:36-37, src/refugia/cli.py:253-269, src/refugia/workspace.py", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Cheapest correct fix: drop `out/` from command-line.md:37 — \"--root is where `data/` lives and where a `specs/` of your own is looked for; the page goes wherever `--out` says, relative to the working directory.\" If you would rather make the docs true, add a `Workspace.out_dir` property and resolve a relative `--out` against it in cli.publish, and say so in the table row at :48. FIXED: The guide says --root is where data/ lives and that publish --out takes its own path.", "batch": ""}
{"id": "fnd-142", "kind": "finding", "title": "`cbsa_type` is filterable and named in the ask prompt, but not in the profile schema page", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "still-open", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["writing-a-profile.md:38-39 lists \"`population`, `state`, `name`, `lat`, `lon`, `fips`, or `metro`\". src/refugia/places/place.py:42 defines `cbsa_type` and :51 defines `metro`; ask/planner.py:26 tells the model \"plus these place fields for criteria: population, state, cbsa_type\" and :78 whitelists it. engine.py:81-82 resolves any Place attribute via hasattr/getattr, so a profile can filter on it today. The two written statements of the vocabulary disagree.", "paths: docs/guide/writing-a-profile.md:38-39, src/refugia/places/place.py:41-53", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Add `cbsa_type` to the list at writing-a-profile.md:38-39 with its values (`metro`, `micro`, or absent outside a CBSA) and one example — `{\"field\": \"cbsa_type\", \"comparison\": \"in\", \"value\": [\"metro\"]}` — and note that `metro` is the CBSA's name and is an empty string for a county in none (place.py:51-53). FIXED: cbsa_type added, and test_guide reads the Place attributes from the class so the vocabulary cannot drift.", "batch": ""}
{"id": "fnd-143", "kind": "finding", "title": "adding-a-spec.md embeds life_expectancy.json \"in full\" and nothing checks it stays that way", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "small", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["docs/guide/adding-a-spec.md:47-70 reproduces src/refugia/specs/life_expectancy.json and calls it \"in full\"; I diffed the fenced block against the file and it is identical today, including the 2025 CHR&R url. tests/test_guide.py checks only paths (:42-53), relative links (:56-64) and snake_case tokens (:67-114) — nothing reads the contents of a file the guide quotes. The guide's stated justification for living in the repo (test_guide.py:1-10) is precisely that the code can fail it.", "paths: docs/guide/adding-a-spec.md:47-70, tests/test_guide.py"], "updated": "2026-09-07", "notes": "Add to tests/test_guide.py: extract the first ```json fence from adding-a-spec.md, json.loads it, and assert it equals json.loads((ROOT/'src/refugia/specs/life_expectancy.json').read_text()) — comparing parsed objects, not bytes, so reformatting does not fail it.", "batch": "prose-guard"}
{"id": "fnd-144", "kind": "finding", "title": "adding-a-spec.md says a spec `key` \"must be a valid Python identifier\"; nothing checks or needs that", "status": "fixed", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "self (the change's author)", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["The field table at adding-a-spec.md:78 states the constraint as a rule, and the page's own \"What is checked, and what is not\" section (:110-120) enumerates the validator without mentioning it. DeclarativeSource._validate (declarative.py:55-88) checks required fields, https scheme, angle brackets on the six DISPLAYED fields, format, direction and aggregate — nothing about identifiers. MetricRegistry.add (registry.py:22-30) only checks for a duplicate key. Nor is the constraint real: keys are dict keys in Python and object keys in the page, never attributes. A spec author is being given an unenforced and unnecessary rule in a table whose other rows are all enforced.", "paths: docs/guide/adding-a-spec.md:78, src/refugia/metrics/sources/declarative.py:55-88", "fixed in the easy-items pass; 361 tests pass and hooks are clean"], "updated": "2026-09-07", "notes": "Reword adding-a-spec.md:78 to what is actually true and useful — \"The identifier a profile weights. Lower-case snake_case by convention; it must be unique across every source\" — or add an `isidentifier()` check to declarative.py._validate and list it under \"What is checked\". FIXED: 'Lowercase, digits and underscores', which is now what _validate enforces.", "batch": ""}
{"id": "fnd-145", "kind": "finding", "title": "tests/test_guide.py's metric-key guard iterates the guide only, so the README can name a metric that no longer exists", "status": "open", "severity": "P2", "release_gate": "nice-to-have", "effort": "trivial", "lens": "user-docs", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "cyc-release-audit-2 adversarial verifier", "refs": [], "depends_on": [], "blocked_by": [], "related": [], "evidence": ["test_guide.py:23 builds DOCS = [*PAGES, README.md] and both the path test (:42) and the link test (:56) are parametrized over DOCS. But test_every_metric_key_the_guide_names_is_registered loops `for page in PAGES` (:104) — the README is excluded. The README names metric keys throughout (`populus_cover`, `juniper_cover`, `min_coverage`, `home_value` …) and is the most-read file in the repo, so renaming a metric would fail the guide and pass the README. That is the drift mode the guard was written to close, left open on the file where it matters most. The finder cited this test as covering the split and did not read the loop.", "paths: tests/test_guide.py:104, tests/test_guide.py:23"], "updated": "2026-09-07", "notes": "Change test_guide.py:104 to `for page in DOCS:` and run it; if the README's illustrative placeholders trip it, add them to the field_names set at :74-102 with a comment rather than narrowing the loop again.", "batch": "prose-guard"}
{"id": "fnd-146", "kind": "finding", "title": "96% of the shipped page is executed by nothing: a syntax error in it passes the whole suite", "status": "open", "severity": "P1", "release_gate": "important", "effort": "trivial", "lens": "tests", "cycle": "cyc-release-audit-2", "finding_kind": "new-defect", "verified_by": "reproduced directly while relaying the re-audit", "refs": [], "depends_on": [], "blocked_by": [], "related": ["fnd-013", "fnd-082"], "evidence": ["template.html carries one script of about 2,200 lines; the conformance kernel between the BEGIN/END markers is ~96 of them", "run_vectors.mjs lifts only the marked slice, so nothing parses the rest", "reproduced: replacing line 1636 with `const oops = ( ;` gives 201 passed while node --check rejects the same file", "everything added outside the kernel — the keyboard route, the requirements panel, the map, saved setups, the Ask panel — has no execution in any check"], "updated": "2026-09-07", "notes": "This was the re-audit's second recommendation and the best value-per-line item it found, and it was missed when the 79 rows were written down — worth recording as a miss of the ledger, not just of the code. The fix is two lines in run_vectors.mjs: extract the whole script body rather than the marked slice and `new Function` it in a try/catch, exiting non-zero on a parse error. node is already a devenv and CI dependency. A jsdom smoke test asserting the table renders is the larger correct version.", "batch": "tests-that-bite"}

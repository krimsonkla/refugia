# Guide

Six pages, in the order most people need them.

## Using it

- **[Using the page](using-the-page.md)** — the published HTML file: sliders,
  requirements, the map, the county and city panels, the keyboard route, and where
  saved setups live.
- **[The command line](command-line.md)** — the four commands, every option and
  default, what a cold fetch costs, and what happens when an upstream moves.
- **[Writing a profile](writing-a-profile.md)** — the whole schema. Weights,
  requirements, normalisation, coverage, home. This is the tool's primary input.
- **[Reading the numbers](reading-the-numbers.md)** — what a score is and is not,
  what the coverage column means, and how to tell how old a page's data is.

## Extending it

- **[Adding a metric with a spec](adding-a-spec.md)** — the no-code tier. Every
  field, what is validated, and what is not.
- **[Adding a metric with an adapter](adding-an-adapter.md)** — for sources a spec
  cannot express. The protocol, the conventions, and why each of them exists.

## Elsewhere

- `../../README.md` — what this is and why it works the way it does
- `../../CONTRIBUTING.md` — setup, the checks CI runs, and the conventions
- `../../DATA_SOURCES.md` — every publisher, its terms, and whether they were read
  or inferred
- `../../SECURITY.md` — what this tool trusts, and what it does not
